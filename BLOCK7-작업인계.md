# BLOCK7 개발 인계 문서

> **Claude Code에서 시작하는 경우** — 저장소 폴더에서 `claude` 를 실행한 뒤
> 첫 메시지로 이렇게 말하면 됩니다:
> *"BLOCK7-작업인계.md 를 읽고 작업을 이어가자. 오늘 날짜 확인해서 버전 새로 시작해줘."*
> Claude Code는 폴더의 파일을 직접 읽고 고치므로 **파일 업로드가 필요 없습니다.**
>
> **웹/앱 대화창에서 시작하는 경우** — 이 파일과 최신 `index.html` 을 **둘 다 업로드**하고
> 같은 말을 하면 됩니다.

최종 갱신: **2026-08-06** / 현재 버전: **v. 26-0806-6**

---

## 0. ⚠️ 새 작업에서 가장 먼저 할 일

1. **오늘 날짜를 실제로 확인**하고 버전을 `v. YY-MMDD-N` 으로 새로 시작한다 (3-2 참조).
2. 아래 **5장 "남아 있는 과제"** 를 읽는다.
3. 데이터 손실 사고(옛 5-1)는 **해결됨**. 다만 아래 7장의 관측 항목을 계속 지켜볼 것.

---

## 1. 프로젝트 개요

- **BLOCK7** — 한국어 PWA 할일 관리 + 성경 말씀 앱
- **단일 파일** `index.html` 하나로 배포 (빌드 파이프라인 없음, GitHub Pages)
- 호스팅: **`https://block7.my/`** (2026-07-30 도메인 이전 완료)
  - 옛 주소 `underkut.github.io/block7/` 는 새 주소로 자동 이동됨
- 저장소: `https://github.com/Underkut/block7`
- 사용 기기: Mac mini + iPhone + iPad mini (+ 갤럭시 안드로이드 테스트)
- **개발자(HB)는 코더가 아님** — 외부 설정은 클릭 순서를 하나하나 짚어줄 것.
  코드 이야기를 할 때도 "무엇이 왜 문제였는지"를 일상어로 먼저 설명한다.

## 2. 현재 버전: **v. 26-0812-5**

최근 이력
| 버전 | 내용 |
|---|---|
| 26-0731-4 | 대시보드 신규, 인앱 팝업 제거 (이전 인계 시점) |
| 26-0731-5 | **5-1 동기화 덮어쓰기 수정** — rev 트랜잭션 + 3자 병합 + 자동 백업 |
| 26-0731-6 | **5-2 중복 구절 정리 도구** (약칭·정식 합치기) |
| 26-0802-1 | 구버전 기기 덮어쓰기 방어 + 유휴 기기 양보 규칙 (7장) |
| 26-0802-2 | **구글 시트 대량 삭제 사고 수정** (아래 6장 "구글 시트 동기화") |
| 26-0802-3 | 타일뷰 핀치 중 말씀메뉴 차단 · 알림 전체화면을 타일뷰 위로 · Deeper 링크 기본 브라우저 · 팝업 닫기를 우상단 × 로 · 개발본 배포(`/index-dev.html`) |
| 26-0802-4 | 연락처·태그된 할일 팝업도 우상단 × · 팝업 디자인 규칙 문서화 |
| 26-0802-5 | **반응 카운터 0 표시 수정**(장절 표기 정규화, `tests/test_count.js`) · Deeper/Even 카운트 경쟁 수정 · **할일 입력 중 ←/→ 가 날짜를 넘겨 어제로 저장되던 사고 수정** · 암송 기록 팝업 분리 · 스닉픽 본문 표시 · 서비스워커 자기 복구 |
| 26-0802-6 | Even Deeper 는 링크 열기 설정에서 제외(ChatGPT 앱 연결 유지) · 스닉픽 한 줄 폭 의미 바로잡음 |
| 26-0802-7 | 스닉픽 한 줄 폭: 롤링피커 · **기기별 저장**(`b7v1_sneakmaxw`) · 즉시 반영 |
| 26-0802-8 | 스닉픽 한 줄 폭 최소 300 · 100 단위 |
| 26-0803-1 | 알림 전체화면: 덮었던 화면 숨겼다 복원 · iOS 절전 깨어남 감지(visibilityState 멈춤 버그 대응) |
| 26-0803-2 | **첫 화면 빠른 그리기**(5-3) — 로컬로 먼저 그리고 클라우드 도착 후 병합·재렌더 (`tests/test_boot.js`) |
| 26-0803-3 | Enter/Tab 분리 · 전체화면 스와이프 굳음 수정 · 말씀 목록 탭/롱터치 · 로고 메뉴 동기화 버튼 |
| 26-0803-4 | 타일뷰 태그 롤링 제목 · 회원가입 버건디 |
| 26-0803-5 | **알림 경로 전면 재작성** — 토큰 기반 중복막기 + 확인될 때까지 재시도 + SW가 알림 표시 직접 담당 + foreground 알림 표시 (`tests/test_notif.js`) |
| 26-0803-6 | 알림: 말씀 목록이 준비되기 전에 포기하지 않도록 · `client.navigate()` 제거 · 진단 기록에 개발본 표시 |
| 26-0803-7 | 아이폰 알림 진단 기록이 늘 비어 있던 문제 (캐시 스토리지가 안 남는다) |
| 26-0803-8 | 아이폰: 전달용 기록·진단 기록을 **IndexedDB** 로 (캐시는 보조) |
| 26-0803-9 | 알림 클릭 처리가 저장소를 기다리다 통째로 멈추던 문제 — 저장을 뒤로, 모든 저장에 시간 제한 |
| 26-0803-10 | 타일뷰 태그/성경책 필터 롤링피커 + 구절 수 표시 · 로그인 로고 버건디 · **PC ESC = 닫기(이전 화면)** · **전체화면을 닫으면 이전 화면이 그대로 남는다** |
| 26-0803-11 | **성경책 이름을 장:절 앞까지로** — `요한1서 5:14` 의 책 이름이 `요한` 으로 잘려 요한1·2·3서가 목록 맨 뒤(요한계시록 뒤)로 밀리던 오류. 약칭·한글 숫자 표기(`요일`, `요한일서`)도 정식 이름으로 편다 (`tests/test_book.js`) |
| 26-0805-1 | **설정 등급(이지·미드·파워)** — 톱니 옆 아이콘 3개로 설정창에 보일 양을 고른다. 요소마다 `data-lv`(mp=미드·파워, p=파워)를 달고, 이지는 버튼탭 자체를 감춘다. 일반설정창 항목 순서·문구 정리, 구간 알림 제목 문구는 알림탭으로, 할일뷰 말씀 표시는 말씀 설정창으로 이관 (`tests/test_uilevel.js`) |
| 26-0805-2 | 등급 아이콘을 헤더 정중앙으로 · 연락처를 할일 입력 위치 아래로 · **시간 개념 없음 구간** — 직접 추가한 구간에만 주는 체크. 켜면 시각 판정(구간 시작 알림·지금 구간·말씀 반응 기록 소속)에서 통째로 빠진다 (`_secNoTime`) |
| 26-0805-3 | 등급 아이콘을 **동물 얼굴**로 — 이지=토끼 · 미드=곰 · 파워=사자(육각 방패 갈기). 눈·코 없는 선 실루엣, 선 굵기 0.75px |
| 26-0805-4 | 등급 아이콘 간격 1.5배 · **PC 설정창을 세로 가운데 정렬에서 위 고정으로** — 등급을 바꿔 내용이 짧아져도 헤더가 제자리에 있고 아래쪽만 줄어든다 |
| 26-0806-1 | 말씀 설정 뷰탭 '구절'→'장절' · 말씀 위젯 항목 기본값(본문·장절만) + **글자 크기 1·2·3** · 위젯 행을 누르면 그 장절 전체화면 · '자동으로 다음 구절'을 **매일 / 매 시간구간** 두 토글로 (`tests/test_verse_widget.js`) |
| 26-0806-2 | **유저 모드** — 이지/미드/파워 아이콘을 동물(토끼·곰·사자)과 사각(한 칸·두 칸·네 칸) 중에 고른다. 일반설정창 뷰탭 첫 항목. 사각은 10×10, 선 0.75, 간격은 동물과 같게 (`_UILV_ART`) |
| 26-0806-3 | 유저 모드(아이콘 모양 고르기)를 **파워 전용**으로 · 기본값 `uiLevelIconSet:'animal'` 을 표에 명시 |
| 26-0806-4 | '시간 개념 없음' 체크를 풀어도 시각 픽커가 돌아오지 않던 버그 (체크 후 `renderSectionEditor()` 를 안 불렀다) |
| 26-0806-5 | (되돌림) `confirm()` → `appConfirm` 교체. **근거가 틀렸다** — 아이폰에서 잘 동작한다고 HB가 확인 |
| **26-0806-6** | 0806-5 되돌림 (네이티브 `confirm()` 유지) · **삭제 × 는 직접 추가한 구간에만** — 기본 6개는 지울 수 없다 |
| 26-0806-7·8 | **시간구간 경계선 모델** (5-0) — 구간 사이 경계 하나로 겹침·빈틈을 없앴다 (`tests/test_secs.js`) |
| 26-0807-1~3 | 말씀 반응 토스트를 아이콘 전용으로 · 크게 · 두근거림 · 더블탭에도 적용 |
| 26-0807-4 | 목록에서 연 전체화면이 목록 뒤에 깔리던 문제 — `_vfHideCovers()` 에 `vAggModal` 외 3개 추가 |
| **26-0810-1** | **말씀카드 위젯** (5-A) — 전체화면 카드를 2·3단 위젯으로. 카드 인스턴스(`card#<id>`) 도입, 목록카드/일반카드, 위젯마다 배경·글자 크기, 목록에서 연 전체화면이 그 목록 안에서 돈다 (`tests/test_verse_card.js`) |
| **26-0810-2** | 말씀카드 다듬기 — 갈래(필터) 카드, 카드 자리를 장절로(카드끼리 안 흔들림), 손가락 hover 잔상, 전체화면 장절 눌림 복구, 반응 토스트 통일, PC 좌우 화살표, 카드별 좌·우 하단 켜고 끄기 |
| **26-0810-3** | 손끝을 따라오는 좌우 스와이프 · 위젯 설정 '사용 안 함'을 세 줄로 + '말씀카드' 만들기 칩(끌어다 놓아도 그 자리에 남는다) · **운영본 배포** |
| **26-0810-4** | 필터를 **교집합**으로 (좋아요 목록 ∩ 그 필터) · 헤더 필터 이름을 button 으로(PC 클릭 복구) · 카드 밖에서 놓으면 되돌아왔다 사라지던 모션 · 카드 설정에 구역 전체 스위치 · 말씀설정 뷰탭의 카드 항목 제거 · **운영본 배포** |
| **26-0810-5** | **동기화 — 낡은 버전이 '모르는 것'을 지우던 사고 수정** (7-2-2). 모르는 위젯 보관 · 낮은 버전이 쓴 문서는 base 없이 되살림 · 앞으로 불러온 직후는 양보 · 낡은 탭 즉시 자가 갱신 · 원격 반영 뒤 설정창 버튼 갱신(5-3) (`tests/test_syncver.js`) |
| **26-0811-1** | 말씀카드 설정 다듬기 — 제목 줄임(테마·글자 크기) · 글자 크기를 '태초에…' 네 단계 실물 크기로 · 새 카드 기본값(80·좌우 하단 꺼짐) · **전체 스위치를 상위 개념으로**(하위 값 보존) · 반응 줄에 아이콘 |
| **26-0811-2** | 자잘한 것들 — 반응 토스트 1.5초·Even 성공 안내 제거 · **롱터치 메뉴가 손 떼면 닫히던 것** · 개발자용 대분류→구글시트 셀 열기 · 등급 바꿔도 보던 탭 유지 · 등급 설명 말풍선 가림 · 개발본 탭 제목 · 타일뷰 기본 정렬 · 모음 검색에 태그 · 카드 헤더 글자 크기 −/+ |
| **26-0811-3** | 반응 토스트 0.5초 · **좌우 넘김 영역이 우하단 반응 버튼을 가리던 것**(본문 안으로, 폭은 1/4) · 시트를 구글 앱으로 열기 · 등급 설명 말풍선 글자색 · 헤더 −/+/3줄 크기·중심 맞춤 · 필터 행의 … 제거 |
| **26-0811-4** | 글자 크기 −/+ 를 **헤더에서 카드 안 우상단으로**(16×16) — 헤더가 좌·가운데·우 셋뿐이라 목록카드의 가운데 아이콘이 정확히 가운데로 돌아왔다 · 좌우 넘김 영역 위쪽 26px 을 비워 −/+ 와 안 겹치게 · 아이폰도 시트 앱을 먼저 부른다(`googlesheets://`, 안 열리면 사파리) |
| **26-0811-5** | 말씀카드 본문 글자 크기 **다섯 단계**(1.25 추가) · 크기 고르는 버튼이 잘리지 않게 글자 폭대로 놓는다(`.ts-row`) |
| **26-0811-6** | 아이폰에서 **앱과 사파리가 둘 다 열리던 것** — 시간을 재서 사파리를 여는 판정을 없앴다 |
| **26-0812-1** | 공유 이미지 저장 파일명의 장절을 **세 자리로 통일** (4:6 → 004006) |
| **26-0812-2** | 그 자리수를 책마다 다르게 — 장은 시편만 3자리, 절은 시편 119장만 3자리, 나머지는 2자리 |
| **26-0812-3** | 파일명을 더 짧게 — 성경책은 공식 약어(창·삼상·요일), 앞머리는 `B7` |
| **26-0812-4** | **핀치로 글자 크기** — 전체화면·말씀카드에서 두 손가락으로 벌리고 오므린다 (롱터치 메뉴는 그동안 안 뜬다) |
| **26-0812-5** | 핀치를 **벌린 만큼 그대로** 따라오게(사이 단계 추가) · **손가락이 하나씩 떨어질 때 말씀이 넘어가던 것** |

---

## 3. 매 작업마다 반드시 지킬 규칙 ⚠️

### 3-1. 항상 2개 파일을 함께 전달
- `index.html` — **운영본** (`DEV_MODE = false`)
- `index-dev.html` — **개발본** (`DEV_MODE = true`, Firebase 미연결)
- (`404.html`, `firebase-messaging-sw.js`, `manifest.json`, `manifest-dev.json`, `og-image.png` 은 건드렸을 때만)

만드는 법: **`./tools/make-dev.sh`** (손으로 고치지 말 것). 네 곳을 바꾼다 —
`DEV_MODE` false→true / 2번째 줄 `[production]`→`[DEV — no Firebase]` /
`manifest.json`→`manifest-dev.json` / 앱 이름 `BLOCK7`→`BLOCK7 DEV`

**개발본도 저장소에 함께 커밋한다.** 그래야 `https://block7.my/index-dev.html` 로
PC·아이폰 어디서나 바로 열린다. 아이폰에서 그 주소를 "홈 화면에 추가" 하면
`manifest-dev.json` 의 `start_url` 덕분에 **BLOCK7 DEV** 아이콘이 운영본과 따로 생긴다.
- ⚠️ 개발본은 Firebase 미연결 → **푸시 알림은 개발본에서 확인 불가.** 운영본에서만 된다
- ⚠️ 홈화면 앱으로 켰을 때만 도는 코드(`navigator.standalone`)는 브라우저 탭에서 확인 불가

### 3-2. 버전 번호 = `v. YY-MMDD-N`
- **반드시 실제 현재 날짜로** (반복 지적받은 실수. 이전 세션 날짜를 그대로 쓰지 말 것)
- `N` 은 같은 날 몇 번째 빌드인지
- 두 군데를 **항상 같은 값**으로:
  1. 2번째 줄 주석 `<!-- BLOCK7  v. YY-MMDD-N  [production]  -->`
  2. JS 상수 `const APP_VERSION = "v. YY-MMDD-N";`
- ⚠️ 이 값은 이제 **클라우드에도 기록**되어 낡은 탭 자동 갱신 판정에 쓰인다(7-2).
  문자열 비교로 판정하므로 형식을 절대 바꾸지 말 것.

### 3-3. 출력 파일명은 항상 `index.html`

### 3-4. 배포 전 검증 (매번, 4종 모두)
1. 인라인 `<script>`를 정규식으로 추출해 각각 `node --check`
2. `const ST=load()` 가 정확히 1개
3. `<div>`/`</div>`, `<button>`, `<svg>` 균형
   - ⚠️ `<button>` 은 원래부터 1개 불균형(주석 안 예시 태그) — 정상
4. **미정의 참조 스캔** — 함수·상수·HTML 핸들러가 전부 정의돼 있는지

검증 스크립트는 세션마다 새로 만들어 왔다. **Claude Code에서는 저장소에
`tools/verify.py` 로 두고 재사용하는 것을 권장** (매번 다시 짜는 낭비 제거).
미정의 참조 스캔 시 주의: HTML 핸들러 속성에서 함수명을 뽑을 때
`.`으로 시작하는 메서드 호출과 `if/for/return` 같은 키워드를 제외해야
오탐이 안 난다.

> **HTML 블록을 잘라낼 때는 div 균형을 반드시 다시 셀 것.**
> 2026-07-31 작업 중 인앱 팝업 섹션을 지우다 범위가 넘쳐
> **알림 탭 전체(말씀 푸시 설정 + 진단 기록)가 통째로 삭제**됐다.
> 균형 검사에서 잡아 이전 빌드에서 복원했음.

### 3-5. 큰 UI 변경은 먼저 제안하고 확인받기
### 3-6. 데이터 계층(저장·동기화)은 급하게 고치지 말 것 — 손실이 곧바로 발생함

**동기화·병합 로직을 고칠 때는 반드시 시나리오 테스트를 함께 쓴다.**
병합 엔진 함수들을 파일에서 추출해 Node로 돌리는 방식이 잘 작동했다
(실제로 이 방식으로 정식 표기 판정 버그를 잡았음). Claude Code에서는
`tests/` 폴더에 두고 회귀 테스트로 계속 굴릴 것. 현재 보유한 테스트:
- 3자 병합 20건 (`test_merge.js`)
- 중복 구절 정리 18건 (`test_dedupe.js`)
- 구버전·유휴 기기 방어 14건 (`test_stale.js`)
- 구글 시트 동기화·대량삭제 방어 23건 (`test_sheet.js`)

### 3-7. 미니멀 디자인 원칙
- 아이콘에 불필요한 테두리/박스 금지
- 요소를 나란히 추가하지 말고 **교체**
- 얇은 산세리프, 절제된 효과
- 보조 동작은 **롱터치/우클릭** 패턴으로 일관되게
- UI 문구는 전부 한국어, 존댓말, 부드러운 어조

---
## 4. 기술 스택 & 자격증명

### Firebase (프로젝트 `block7-8f24e`, **Blaze 요금제**)
```
apiKey: "AIzaSyDeMIzExHqyeqHQrqpPbcOJqYO9a7qmrkE"
authDomain: "block7-8f24e.firebaseapp.com"
projectId: "block7-8f24e"
messagingSenderId: "517626689480"
appId: "1:517626689480:web:92ea52eeebc24277ef72fd"
```

**⚠️ Firestore 데이터 구조**
- 상태 저장 위치: `users/{uid}/data/state`
- **내용은 `{ json: "문자열" }` 한 칸에 통째로** — `JSON.parse(raw.json)` 필요
- **`users/{uid}` 문서 자체는 비어 있음** → `collection('users').get()` 은 0개 반환.
  반드시 **`listDocuments()`** 사용
- 토큰: `users/{uid}/pushTokens/{token}` — 이제 **기기별로 여러 개** 공존
  (문서에 `devId`, `label` 필드 있음)
- 기기 로컬 전용 키(`b7v1_uiscale`, `b7v1_layform`, `b7v1_devid`,
  `b7_devNotesTrash` 등)는 localStorage에만 — 클라우드 동기화 금지

### 배포된 서버 함수
| 이름 | 방식 | 역할 |
|---|---|---|
| `block7Notify` | 스케줄 1분마다 | 할일 알림 (맥미니 `functions` 폴더에 소스) |
| `block7TestPush` | HTTP | 테스트용 |
| `versepush` | HTTP + Cloud Scheduler 1분마다 | **말씀 푸시** (Cloud Run 인라인 편집기) |

- `versepush` 열쇠: `?key=block7-secret-2026`
- 진단: `...run.app?key=block7-secret-2026&test=1` (시각 무시하고 강제 발송)
- **⚠️ Node.js 20 런타임이 2026-10-30 사용 중단** → 그 전에 22로 올려야
  `block7Notify`(할일 알림)가 멈추지 않음

### EmailJS (개발자 쪽지)
```
service: service_bf2idow / template: template_gorsguw
publicKey: RBOsbdkZDO793ovQq / 수신: denartist@gmail.com
```
첨부는 Firestore `devNotes`에 저장, 메일은 알림용

### 코드 상 접근
- `window._fbDB` = Firestore, `window._fbUser` = 로그인 사용자
- 개발자 계정: `denartist@gmail.com` (`_isDevAccount()`)

---

## 5. 남아 있는 과제

### 5-0. 시간구간 개편 — **v26-0806-8 개발본에 구현됨** (HB 확인 대기)

HB와 2026-08-06에 합의한 내용. 제안서: `claude.ai/code/artifact/7a059eaf-945d-4396-8e47-ef19dae1e391`

**왜** — 지금은 구간마다 시작·마침을 따로 넣어 **겹치거나 빌 수 있다**
(오전 09–12, 점심 11–13 → 11:30 이 양쪽에 속함). 순서를 바꾸면 구간이
자기 시각을 들고 이동해 순서와 시간이 어긋난다.

| 항목 | 확정 내용 |
|---|---|
| 시각 입력 | 구간 **사이에 경계선 하나**. 시작=위 경계, 마침=아래 경계(마지막은 첫 경계로 돌아감) |
| 겹침 방지 | 픽커 목록을 **앞뒤 경계 사이 값으로만** 채운다 → 잘못 넣는 것이 불가능 |
| 순서 | 경계는 **구간이 아니라 자리(위치)** 가 소유. 끌어 옮기면 그 자리의 시각을 받는다 |
| ⠿ 손잡이 | **커스텀 구간만.** 프리셋은 순서 이동 없음 |
| 삭제 × | **커스텀 구간만** (v26-0806-6에 이미 적용) |
| 새 커스텀 | 위아래 경계가 **같은 값(0분)** 으로 생성 → 경계 하나를 옮기면 그때부터 시간을 가진다 |
| 시간 없음 | **체크박스를 없앤다.** 0분이면 자동. 그 행에 회색으로 "시간 없음"만 표시 |
| 안전장치 ① | "0분"과 "하루 전체"가 구분 안 되는 순간이 있다(구간이 1개일 때). **화면에는 없애되 데이터에는 자동 표시를 남긴다** |
| 안전장치 ② | **마지막 남은 시간 있는 구간은 0분으로 못 만든다** (하루에 갈 곳이 없어짐) |
| 안전장치 ③ | 0분 구간은 이웃과 시작 시각이 같아 **서버가 알림을 두 번 보낸다** → `functions/index.js` 도 고치고 재배포 |
| 일정 | **시각이 구간을 정한다** (말씀 반응과 같은 방식). 시각을 바꾸면 그 구간으로 자동 이동 |
| 일정 · 시각 없는 것 | 시각이 없으면 계산 근거가 없다 → **등록한 구간에 그대로 머문다** |
| 일정 · 픽커 | 구간에서 `+` 로 등록하면 시·분 목록이 **그 구간 범위 안에서만**, 기본값은 구간 시작 |
| 일정 · 재배치 | 경계를 바꾸거나 구간을 지우면 **시각 있는 일정을 전부 다시 배치** |
| 일정 · 순서 | 시각 있는 일정은 **시간순 고정**. 끌어 옮기는 것은 **시각 없는 일정**뿐이고, 그것은 시각 있는 일정들 **사이 어디로든** 들어갈 수 있다 |
| 커스텀 삭제 | 확인창에서 **보관 / 깨끗이 지우기** 선택. 대상은 **할일 · 말씀 반응 · 시각 없는 일정** (시각 있는 일정은 자동 재배치되므로 대상 아님). 보관하면 `+ 시간 구간 추가` 에 "보관해 둔 구간 되살리기" 가 함께 보인다 |

⚠️ **데이터 구조 확인 결과** — 일정은 `ST.days[날짜].events[구간id]` 로 **구간 안에 저장**돼 있다.
"시각으로 그때그때 찾는다"가 **아니다.** (2026-08-06에 이걸 잘못 말했다가 바로잡음)
일정은 시각 없이도 저장된다 — 등록창의 '시각' 토글을 끄면 `time` 이 `null` 이다.

**만드는 순서** — ① 경계선 모델 + `tests/` 시나리오 ② 일정 재배치 ③ 삭제 선택.
데이터가 실제로 이동하므로 **시나리오를 먼저 넣고** 시작할 것.

#### 구현 결과 (v26-0806-7)

시나리오는 `tests/test_secs.js` 에 먼저 넣고 시작했다 (93개). 코드는 `index.html` 의
`// ══════ 시간 구간 = 경계선 모델` ~ `// ══════ 경계선 모델 끝 ══════` 한 덩어리에 모여 있다.
테스트가 그 표시 문자열로 잘라 쓰므로 **표시를 지우거나 바꾸지 말 것.**

| 만든 것 | 어디에 |
|---|---|
| 경계 펴기·정규화 | `_secOffsets` / `_secNormalizeTimes` — 구간 목록이 바뀔 때마다 부른다 |
| 픽커 범위 | `_secBoundaryChoices`(경계) · `_secTimeChoices`(일정 등록창) |
| 일정 재배치 | `_reassignTimedEvents` · 정렬은 `_sortEventsKeepingTimeless` |
| 마무리 묶음 | `_secsCommit()` = 정규화 → 재배치 → 저장. 구간을 건드린 뒤엔 이것만 부르면 된다 |
| 보관·되살리기 | `_secArchiveCapture` / `_secStripData` / `_secArchiveApply`, 저장 자리는 `ST.secArchive` (최대 20개) |
| 순서 바꾸기 | `_secMoveTo(secs,from,to)` — ⠿ 드래그가 하는 일은 전부 여기 모여 있다 |
| 화면 | `_makeBoundaryRow` / `_makeBoundaryRoll` / `updateSectionBoundary` / `renderSecArchive` |

⚠️ **구간을 끌어 옮길 때 시각이 바뀌는 것은 옮긴 구간 하나뿐이다** (v26-0806-8에 고침).
끼어들어 간 '틈'의 경계 값을 복제해 자기 위·아래 경계로 삼으므로, 그 자리에서 0분으로
들어가고 틈 양옆 구간의 시작·마침은 하나도 흔들리지 않는다. 0806-7에서는 경계를 '자리'에
붙박아 둬서, 구간 하나가 끼어들면 아래쪽 구간들이 남의 시각을 하나씩 넘겨받으며 통째로
밀렸다. 원래 있던 자리에 남는 시간은 바로 위 구간이 이어받는다 (구간을 지울 때와 같은 규칙).

설정 화면의 한 줄은 **⠿ · 이름 · 색 · ★ · ×** 순서다. ⠿ 와 × 는 커스텀 구간에만 주되,
기본 구간에는 같은 크기의 **숨긴 자리**를 둬서 색·★ 가 모든 줄에서 같은 세로줄에 선다.
지우면 줄마다 어긋난다.

**저장 형태는 그대로 `startTime`/`endTime` 두 값이다.** 앱 곳곳과 클라우드 함수가 이 둘을
읽기 때문에 바꾸지 않았다. 다만 `endTime` 은 이제 **다음 구간의 `startTime` 을 베낀 값**일
뿐이고 `_secNormalizeTimes()` 가 늘 다시 맞춘다. **`endTime` 을 손으로 넣지 말 것.**

정리해야 할 지점 — 옛 데이터(겹침·손으로 켠 '시간 개념 없음')는 세 곳에서 자동으로 눕는다:
`syncSecsFromState()`(로컬 로드) · `applyRemoteState()`(다른 기기가 보낸 것) · `importBackup()`(백업 불러오기).

안전장치가 실제로 어떻게 지켜지는지:
- ① 구간이 하나뿐이면 시작=마침이어도 `noTime` 을 붙이지 않는다 (하루 전체라는 뜻)
- ② 경계 길이의 합이 늘 1440이 되게 펴기 때문에 **시간 있는 구간이 0개가 되는 상태는 구조적으로 생기지 않는다.**
  그래도 규칙이 바뀌면 바로 걸리도록 `_secWouldEmptyDay()` 로 후보에서 걸러 낸다
- ③ `functions/index.js` 에 0분 구간 건너뛰기 + 같은 시각 한 번만(`firedSlot`) 을 넣었다.
  ⚠️ **클라우드 함수는 따로 배포해야 적용된다** (`firebase deploy --only functions`)

⚠️ 새 구간은 맨 아래에 **첫 경계와 같은 값(0분)** 으로 태어난다. 하루의 끝에 붙기 때문에
다른 구간의 시간을 뺏지 않는다. 이걸 가능하게 하는 것이 `_secOffsets()` 의
"앞 경계보다 이른 값은 한 바퀴 돌아온 자리로 읽는다" 한 줄이다. 그 줄을 지우면
새 구간이 **바로 위 구간의 시간을 통째로 가져간다.**


### 5-A. 말씀카드 위젯 (0810-1) — **v26-0810-1 개발본에 구현됨** (HB 확인 대기)

HB와 2026-08-07에 합의. 전체화면 말씀 카드를 2·3단 뷰 안의 위젯으로 쓰는 기능.

**두 종류 — 차이는 "어떤 말씀 목록 안에서 도는가" 하나뿐이다.**

| | 목록카드위젯 | 일반카드위젯 |
|---|---|---|
| 도는 범위 | 그 반응 목록 안 (좋아요/암송/Deeper/Even) | 말씀모음 설정에서 고른 말씀 전체 |
| 시작 | 말씀목록 위젯 헤더 맨 우측(개수 표시 오른쪽) **정사각형 버튼** | 하단 여백 **+ 버튼 → 말씀카드** |
| 헤더 중앙 | 그 반응 아이콘 | (없음) |
| 우상단 | 햄버거 → 목록 위젯으로 복귀 | (없음) |

| 항목 | 확정 내용 |
|---|---|
| 개수 제한 | **없다.** 같은 종류를 여러 개 놓을 수 있다 |
| 위젯 높이 | **드래그로 조절** (기존 `.rp-vresize` 방식) |
| 배경 설정 단위 | **위젯 하나하나마다 따로.** 종류별이 아니다. 일반카드위젯도 같다 |
| 기본 배경 | 따로 정하지 않으면 '말씀 설정 → 전체화면' 탭 설정을 따른다 |
| 설정 팝업 진입 | ① 목록위젯 헤더 여백 롱터치/우클릭 ② 헤더 필터를 연 뒤 필터 행 맨 우측 톱니바퀴 |
| 설정 팝업 구성 | '전체화면' 탭과 같은 구성 중 **말씀카드 글자크기 · 말씀카드 테마** 두 가지만. '반응 카운터'·'링크 열기'는 전체화면 설정을 따라간다는 안내와 현재 값을 그 자리에 표시 |
| 표시 항목 켜고 끄기 | 말씀설정 **'뷰' 탭**에 알약버튼. 좌하단(대분류·소주제·태그) / 우하단(좋아요·암송·Deeper·Even) |
| 버튼 기능 | 전체화면과 동일. 좌하단=그 필터로 타일뷰 진입, 우하단=반응 카운트·링크 |
| 우클릭/롱터치 메뉴 | 전체화면·말씀목록 위젯과 동일하게 |

**2-1-1-2 (목록 → 전체화면)** — 좌상단 메뉴 LIST 에서 목록에 들어가 말씀을 눌러
전체화면을 열 때도 같은 규칙: 그 목록 안에서 돌고 / 헤더 중앙에 반응 아이콘 /
우상단 햄버거로 목록 복귀.
(⚠️ 이때 전체화면이 목록 뒤에 깔리던 버그는 **v26-0807-4 에서 이미 고쳤다** —
`_vfHideCovers()` 에 `vAggModal` 외 3개를 넣었다. 햄버거·헤더 아이콘은 아직 없다)

⚠️ **먼저 풀어야 할 구조 문제 — 위젯이 "타입 하나 = 위젯 하나" 다.**
`_lay()` 의 `cols` 는 `['todo','likeList',…]` 처럼 **타입 문자열 배열**이고,
`seen` Set 으로 **중복을 지운다.** 그래서 지금 구조로는 같은 위젯을 두 개 놓을 수 없다.
"개수 제한 없음"을 위해 **인스턴스 개념**이 필요하다. 제안:
- `cols` 항목을 `card#<id>` 형태도 받게 하고 (기존 타입 문자열과 공존)
- `ST.settings.verseCards = { <id>: {kind:'like'|'mem'|'deeper'|'even'|null, theme, textScale, height} }`
  (`kind:null` = 일반카드위젯)
- 중복 제거(`seen`)는 **카드가 아닌 타입에만** 적용

**만드는 순서** — ① 카드 렌더링을 위젯에서 재사용할 수 있게 분리
② 목록카드위젯(전환·햄버거·헤더 아이콘) ③ 일반카드위젯(+ 버튼)
④ 말씀설정 '뷰' 탭 알약버튼 ⑤ 위젯별 배경 설정 팝업.
HB 요청: **①~⑤ 를 다 만든 뒤 한 번에 확인**한다 (중간 단계로 나눠 올리지 않는다).

⚠️ 카드 본문 줄바꿈(`_vfLayoutText()`)은 **컨테이너 폭을 재서** 끊는다.
위젯은 전체화면보다 훨씬 좁으므로 그 폭을 제대로 넘겨야 한다.
CLAUDE.md 의 "폭은 컨테이너에서 잰다" 규칙이 그대로 적용된다.

#### 구현 결과 (v26-0810-1)

시나리오는 `tests/test_verse_card.js` 에 78개. 코드는 `index.html` 의
`// ══════ 말씀카드 위젯 = 카드 인스턴스 모델` ~ `// ══════ 말씀카드 위젯 끝 ══════`
한 덩어리에 모여 있다. 테스트가 그 표시 문자열로 잘라 쓰므로 **표시를 지우거나 바꾸지 말 것.**

| 만든 것 | 어디에 |
|---|---|
| 인스턴스 모델 | `_vcIs` / `_vcIdOf` / `_vcAll` / `_vcGet` / `_vcCreate` / `_vcRemove`. 저장 자리는 `ST.settings.verseCards[<id>]` |
| 도는 범위 | `_vcVerses(cfg)` — kind 없으면 `ACTIVE_VERSES()`, 있으면 `_aggEntriesForKind(kind)` |
| 지금 보여줄 구절 | `_vcCurrent(cfg)` — 일반카드는 **오늘의 구절 + off**, 목록카드는 **목록 맨 앞 + off** |
| 배경 | `_vcPatternKey` / `_vcThemeVars` — 값은 카드 요소에 `--vf-*` 로 직접 칠한다 |
| 글자 크기 | `_vcTextScale(cfg)` (null 이면 전체화면 `vfTextScale` 을 따라감) |
| 줄바꿈 | `_vcLayoutOne(wEl)` — 전체화면과 같은 `_vfWrapFit` 엔진, **폭·높이는 그 카드의 `.vc-inner` 에서 잰다** |
| 높이 | `_rpVCardH` / `_rpSetVCardH` (`rpWidgetHeights['card#<id>']`, 130~700, 기본 220) |
| 목록 ↔ 카드 | `vlToCard(kind)` / `vcBackToList(id)` / `_vcUnplacedForKind(kind, 제외id)` |
| 새 일반카드 | `vcAddCard(col)` — 컬럼 하단 `+` 메뉴(`openRpAddMenu`)의 '말씀카드' |
| 카드 설정 팝업 | `openVcSettings` / `renderVcSettings` / `setVcTheme` / `setVcTextScale` (`#vcSetModal`) |
| 제스처 | `_vcAttachGestures` — 탭=전체화면 / 좌우 밀기=다음·이전 / 롱터치·우클릭=말씀 메뉴 |

**⚠️ 중복 제거(`seen`)는 타입 위젯에만 걸린다.** `_lay()` 안에서 `card#…` 은
`seen` 을 타지 않고 "설정이 있는가"만 본다. 이 한 줄이 "같은 종류를 여러 개"를
가능하게 하는 자리다. 여기에 카드를 다시 끼워 넣으면 두 번째 카드가 조용히 사라진다.

**⚠️ 반응 버튼은 카드에서 가로로 눕힌다** (`.vc-actions{flex-direction:row}`).
전체화면처럼 세로로 쌓으면 5개가 167px 를 먹어서, 220px 카드에서 본문이 한 줄만
남고 나머지는 `…` 으로 잘렸다. 실제로 그렇게 만들었다가 고쳤다.

**⚠️ 색 변형은 장절에서 계산한다** (`_vcHash`). 전체화면처럼 무작위로 뽑으면
위젯이 다시 그려질 때마다 배경색이 깜빡인다.

**목록 ↔ 카드를 오갈 때 설정은 남는다.** 햄버거로 목록에 돌아가도
`ST.settings.verseCards[<id>]` 는 지우지 않고, 다시 카드로 바꿀 때 그걸 되쓴다.
(`_vcUnplacedForKind` 로 찾는다 — 자기 자신을 제외하지 않으면 방금 뺀 카드가
스스로를 찾아 지워버린다. 처음 만들 때 실제로 그 버그가 났다)

#### 0812-4 · 0812-5 — 핀치로 글자 크기 (전체화면 · 말씀카드)

두 손가락으로 벌리면 글자가 커지고 오므리면 작아진다.
`_attachTextPinch(el,{get,steps,set,onStart})` 한 함수를 두 화면이 같이 쓴다.

**손가락을 두 배로 벌리면 글자도 대략 두 배** — 벌린 만큼 그대로 따라간다
(`목표 = 손짓 시작 배율 × 벌어진 배율` → 가장 가까운 칸). 처음(0812-4)에는 "몇 칸
옮길지"를 세었는데 한 칸에 1.35배가 필요해 굼떴다.

**핀치 전용으로 단계를 촘촘히 한다** — `_tsFine(steps)` 가 설정 버튼의 단계
**사이사이**를 `TS_FINE_ALL` 로 메운다. ⚠️ **양 끝(가장 작은 것·가장 큰 것)은
설정과 똑같고 넘지 않는다.**

| 화면 | 설정 단계 | 핀치 단계 |
|---|---|---|
| 전체화면 | 0.5 · 0.6 · 0.8 · 1 | 0.5 · **0.55** · 0.6 · **0.7** · 0.8 · **0.9** · 1 |
| 말씀카드 | 0.5 · 0.6 · 0.8 · 1 · 1.25 | 위 + **1.12** · 1.25 |

사이값(0.7 같은 것)에 멈출 수 있으므로 **설정 버튼은 가장 가까운 것에 불을 켜고**
(`_tsNearest`), 카드 안 **−/+ 는 "지금 값보다 큰/작은 첫 설정 단계"** 를 찾는다
(칸 번호로 세면 사이값에서 한 칸을 건너뛴다).

손 떨림에 값이 깜빡이지 않게 옆 칸으로 옮기는 문턱을 둔다 (`TS_SNAP_HYST` ≈ 6%).

**⚠️ 손가락은 동시에 떨어지지 않는다.** 하나가 먼저 떨어지면 남은 하나가
"새로 미는 손가락"으로 읽혀 **말씀이 넘어가거나 화면이 튄다.** (0812-4 에서 실제로
그랬다.) 그래서 `_tsPinchBusy()` — **핀치 중 + 손가락이 다 떨어지고
`TS_PINCH_TAIL`(450ms) 동안** 다른 제스처를 모두 막는다. 막은 자리는 여섯 곳:

| 어디 | 왜 |
|---|---|
| 전체화면 `touchstart`·`touchmove`·`touchend` | 남은 손가락이 세로 드래그로 읽혀 말씀이 넘어간다 |
| 전체화면 `click`(캡처) | 핀치 끝자락에 딸려 오는 가짜 클릭이 `.vf-nav` 를 눌러 넘어간다 |
| 전체화면 가장자리 뒤로가기(`_initEdgeBack`) | 남은 손가락이 왼쪽에 있으면 화면이 닫힌다 |
| 말씀카드 `touchstart`·`touchmove`·`touchend`·`click` | 가로 밀기·탭으로 읽혀 넘어가거나 전체화면이 열린다 |

**⚠️ 두 번째 손가락이 닿는 순간 롱터치 대기를 끊는다.** 안 끊으면 크기를 바꾸는
도중에 말씀 메뉴가 떠서 제스처가 통째로 끊긴다. 두 화면의 사정이 다르다 —
말씀카드는 원래 `touches.length!==1` 이면 `stopHold()` 후 `active=false` 였지만,
전체화면은 **손가락 수를 아예 안 봤다.**

**⚠️ 미는 도중에 `renderRightPanel()` 을 부르면 안 된다.** 만지고 있던 카드가
새로 그려져 손가락이 붙잡고 있던 요소가 사라진다 → 제스처가 끊긴다.
`vcSetTextScaleLive(id,v)` 가 그 카드 하나만 `_vcLayoutOne` 으로 다시 앉힌다.

`tests/test_pinch.js` 가 단계표·따라오는 정도·양 끝·문턱·손 떼기·막은 자리를
고정한다. 브라우저에서도 실제 두 손가락 이벤트로 확인했다.

#### 0812-1 ~ 0812-3 에서 손본 것 — 공유 이미지 저장 파일명

`_refDigitsPad(ref)` 가 장절 숫자의 **자리수**를 맞춘다. 파일을 이름순으로
늘어놓아도 장절 순서와 맞게 하려는 것이다 (예전에는 `:` 만 지우고 숫자를 있는
그대로 붙여서 4:6 이 `46` 이 되어, 15:29 보다 뒤로 갔다).

자리수는 **"그 자리에 올 수 있는 가장 큰 수"** 에 맞춘다. 더 늘리면 0 만 길어진다.

| | 자리수 | 왜 |
|---|---|---|
| 장 — 시편 | 3 | 150장까지 있다 |
| 장 — 나머지 | 2 | 가장 긴 이사야가 66장 |
| 절 — 시편 119장 | 3 | 176절까지 있다 |
| 절 — 나머지 (시편의 다른 장 포함) | 2 | 100절을 넘는 장은 시편 119장 하나뿐. 그다음이 민수기 7장 89절 |

창세기 4:6 → `0406` · 사무엘상 15:29 → `1529` · 시편 23:1 → `02301` ·
시편 119:176 → `119176`

**이름이 길어지지 않게** 앞머리는 `B7`, 성경책은 **공식 약어**로 적는다 (0812-3).

| 장절 | 파일명 |
|---|---|
| 창세기 4:6 | `B7_창_0406_260812.png` |
| 사무엘상 15:29 | `B7_삼상_1529_260812.png` |
| 시편 119:176 | `B7_시_119176_260812.png` |
| 요한1서 5:14 | `B7_요일_0514_260812.png` |

약어는 `_bookAbbr(name)` 이 만든다. **새 표를 만들지 않고 `_REF_ABBR2FULL`
(약어→정식)을 뒤집어 쓴다** — 표가 둘이면 언젠가 어긋난다. 표에 없는 이름은
줄이지 않고 그대로 둔다. 테스트가 66권 전부를 왕복시켜 확인한다.

절이 여럿인 표기는 숫자 덩어리마다 따로 편다 — `요한복음 1:1,14` → `0101,14`,
`시편 119:9-16` → `119009-016`. 장:절 꼴이 아니면(옛 자료의 `시편 119`) 그대로 둔다.

**⚠️ 책 이름에도 숫자가 있다.** `요한1서 5:14` 에서 첫 숫자(1)를 장으로 보면
`0105` 가 된다. `_bookOfRef` 와 똑같이 **`:` 까지 확인**해야 한다
(`/^(.+?)\s*(\d+)\s*:\s*(.+)$/`). 시편인지 가릴 때는 `_bookCanon` 으로 펴서
견준다 — `시 23:1` 같은 약칭도 시편이다. `tests/test_sharename.js` 가 고정한다.

전체화면과 말씀카드는 `vfShareDo` → `_vfShareImage` → `_shareFileName` 한 길을
지나므로 고칠 곳은 한 군데다.

#### 0811-6 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| ⚠️ 시트 링크에서 앱과 사파리가 **둘 다** 열림 | 0811-5 는 `googlesheets://` 를 부른 뒤 0.8초를 재서 "아직 화면이 그대로면 앱이 없는 것"으로 보고 사파리를 열었다. 그런데 아이폰은 앱으로 넘어가는 데 그보다 오래 걸리고, 그 사이 확인 창이 뜨기도 한다 → 이미 앱이 뜨는 중인데 사파리까지 걸어서, 최종 화면이 사파리가 됐다 |

**시간을 재서 "안 열렸다"고 판정하지 말 것.** 아이폰에서는 어떤 시간을 잡아도
틀린다. 짧으면 앱이 뜨기 전에 사파리를 열고, 길면 사람이 한참 기다린다.
지금은 이렇게 한다:

1. `googlesheets://` 를 부른다.
2. 2.5초 동안 **화면을 벗어난 낌새**를 넓게 본다 —
   `visibilitychange` · `pagehide` · `blur` · `freeze` 중 **하나라도** 걸리거나
   `document.hidden` 이거나 포커스를 잃었으면 → **넘어간 것.** 아무것도 안 한다.
3. 정말 아무 낌새도 없을 때만 안내를 띄우고 그 주소를 `_sheetAppMiss` 에 적어 둔다.
4. **같은 주소를 한 번 더 부르면** 그때 사파리로 넘긴다 (`BibleLinkProvider.open`).

즉 **사파리는 사람이 한 번 더 누를 때만 열린다.** 자동으로는 절대 열지 않는다.
앱이 깔려 있으면 3·4번은 아예 일어나지 않는다.

#### 0811-5 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 글자 크기 한 단계 더 | `VC_TS_STEPS` 에 **1.25** 를 더해 다섯 단계 (0.5·0.6·0.8·1·1.25). 미리보기 글자 `VC_TS_PX[1.25]=19.5` |
| ⚠️ 상·하한을 숫자로 박지 말 것 | `_vcTextScale` 과 `setVcTextScale` 이 `v>=0.45&&v<=1` 로 걸러서, 단계만 늘리면 **새 단계가 저장되지 않는다**(고르면 기본값 0.8 로 되돌아간다). 이제 `VC_TS_MIN`/`VC_TS_MAX` 를 표에서 뽑아 쓴다 — 다음에 단계를 더 늘려도 자동으로 따라온다 |
| 줄 간격 | `LHe=_VF_LH*(1+Math.max(0,1-tScale)*0.72)` — 1 을 넘는 단계에서 괄호 안이 음수가 되면 줄끼리 붙는다. 1 위에서는 더 좁히지 않는다. (전체화면 쪽 같은 줄은 배율 상한이 1 이라 그대로 뒀다) |
| ⚠️ 크기 고르는 버튼이 잘림 | `.ts-btn` 이 `flex:1`(basis 0)이라 폭을 똑같이 나눴다 → 다섯 개가 되자 16px·19.5px 짜리 '태초에…' 가 실제로 잘렸다(실측 71px 글자를 61px 상자에). 이제 **글자 폭 그대로**(`flex:0 0 auto`, 좌우 여백 1px) 놓고 남는 자리는 `.ts-row{justify-content:space-between}` 로 사이 간격에 흘린다. 좁으면 잘리는 대신 아랫줄로 넘어간다 |

**카드가 낮으면 큰 단계가 티가 안 난다.** `_vcLayoutOne` 은 카드 높이에 들어가는
가장 큰 글자를 고르므로, 긴 말씀 + 낮은 카드(기본 220px)에서는 1 단계와 1.25 단계가
같은 크기(13px)로 나온다. 카드를 400px 쯤으로 늘리면 16px → 20px 로 갈린다.
이건 고장이 아니라 자동 맞춤이 원래 그런 것이다 — "왜 커지지 않느냐"는 물음이
오면 카드 높이를 먼저 보라.

#### 0811-4 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| ⚠️ 헤더가 지저분 · 가운데 아이콘이 틀어짐 | 0811-3 에서 −/+ 를 헤더 오른쪽에 넣었더니 **목록카드**는 오른쪽에 −/+ 와 햄버거가 같이 들어가 왼쪽(⋯)보다 훨씬 넓어졌다 → 가운데 반응 아이콘이 왼쪽으로 밀렸다. 이제 헤더는 **좌=⋯ · 가운데 · 우=햄버거** 셋뿐이고, 좌우 자리(`.vc-head-slot`)를 22px 로 **같게 고정**한다. 일반카드는 오른쪽 자리를 비워 두기만 한다(없애지 않는다 — 없애면 또 틀어진다) |
| −/+ 새 자리 | 카드 **안쪽 우상단** (`.vc-zoom`, `top:5px;right:5px`), 버튼은 16×16 (`.vc-zbtn`). ⚠️ **`.vc-slide` 밖, `.vc-body` 바로 아래**에 그린다 — 슬라이드 층 안에 넣으면 좌우로 밀 때 글자와 같이 딸려가 버린다 |
| ⚠️ 좌우 넘김이 −/+ 를 덮음 | −/+ 가 카드 안으로 들어왔으니 화살표 영역 위쪽을 비워야 한다. `.vc-arrow` 를 `top:0` → **`top:26px`**. (실제로 재서 확인: 화살표 상단 26px, −/+ 는 5~21px → 안 겹침) |
| 아이폰 시트 앱 | 사파리를 거치지 않고 **`googlesheets://` 로 바로** 부른다. 앱이 없으면 화면이 그대로 남으므로 0.8초 뒤 사파리로 넘긴다(예전 동작). 넘어갔는지는 `visibilitychange`/`pagehide` 로 본다 |

#### 0811-3 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 반응 토스트 | 1.5초 → **0.5초**. 뜰 때의 두근거림도 0.9s → **0.45s** 로 함께 줄였다 — 이게 사라지는 시각보다 길면 애니메이션 도중에 잘려 덜컹거린다 |
| ⚠️ 좌우 넘김이 반응 버튼을 가림 | 화살표 영역이 `.vc-body` **전체 높이**를 덮어서, 우하단 반응 버튼 중 맨 오른쪽 것이 눌리지 않았다. 이제 `.vc-inner`(본문 영역) 안에만 둔다 — 아래 버튼줄과 절대 겹치지 않는다. 가로는 반대로 넓혀 **카드 폭의 1/4**(`width:25%`). 장절은 `z-index:5` 로 올려 눌림을 뺏기지 않게 했다 |
| 시트를 앱으로 | `_openSheetUrl()` — 안드로이드는 `intent://…package=com.google.android.apps.docs.editors.sheets` (앱이 없으면 `browser_fallback_url` 로 웹), 아이폰은 `BibleLinkProvider.open()` 으로 사파리에 넘겨 유니버설 링크가 시트 앱을 잡게 한다. PC 는 그대로 새 탭 |
| ⚠️ 등급 설명이 빈칸으로 보임 | 말풍선 배경이 `var(--s3, rgba(0,0,0,.82))` 인데 **`--s3` 는 정의돼 있지 않다** → 늘 어두운 배경. 글자색은 `var(--tx)` 라 **밝은 테마에서 어두운 글자 + 어두운 배경**이 되어 안 보였다. 배경·글자를 고정색으로 못박았다 |
| 헤더 −/+ · 3줄 | 글자(`−` `＋`)는 서체마다 크기·중심이 달라 3줄 아이콘과 어긋난다 → **그림으로** 바꾸고 셋 다 같은 상자(20×18)·같은 선 굵기(1.6). 목록 위젯의 계단형 정렬 아이콘도 2.4 → 1.6 |
| 필터 행의 … 제거 | 카드 설정은 카드 헤더의 ⋯ 하나로만 들어간다 (`openVlCardSettings` 삭제) |

#### 0811-2 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 반응 토스트 | 2.5초 → **1.5초**. Even Deeper 의 '복사했어요'는 없앴다(곧바로 ChatGPT 로 넘어가 볼 겨를이 없다). **복사 실패 안내는 남겼다** — 그때는 사용자가 직접 입력해야 한다 |
| ⚠️ 롱터치 메뉴가 손을 떼면 닫힘 | "연 지 450ms 안이면 무시"로 막고 있었는데 롱터치 판정이 **500ms** 라 조금만 더 붙잡으면 그 시간을 넘겨 곧바로 닫혔다. 이제 시간이 아니라 **새로 누른 적이 있는지**로 본다 (`_menuArmOnNextPress`) — 메뉴를 연 그 손가락이 떨어지는 것은 새로 누른 것이 아니다. 말씀 메뉴·목록 메뉴 둘 다 적용 |
| 개발자용 시트 열기 | 전체화면 **대분류 롱터치/우클릭** → 그 말씀이 온 구글 시트를 `#gid=…&range=A행:F행` 으로 연다 (`_sheetUrlForVerse`). 행 번호는 v26-0811-2 부터 가져올 때 함께 적어 두므로(`row`), **그전에 가져온 말씀은 한 번 동기화해야** 셀까지 잡힌다. 일반 사용자에게는 아무것도 달지 않는다 |
| ⚠️ 등급 바꾸면 버튼탭으로 튐 | `_currentSettingsTabIdx` 는 **보이는 탭 목록**(`_stabList`)의 자리인데 전체 목록(`SETTINGS_TABS`)에 그대로 넣고 있었다. 이지에서는 '버튼'이 빠져 '구간'이 보이는 목록의 3번인데 전체 목록 3번은 '버튼'이라 그리로 튀었다. 등급을 바꾸기 **전에** 탭 이름을 붙잡아 둔다 |
| 등급 설명 말풍선 가림 | `.settings-hd` 에 `z-index:30` (탭바가 10) |
| 개발본 탭 제목 | `make-dev.sh` 가 `<title>` 도 `BLOCK7 DEV` 로 바꾼다 (되돌리기는 `make-prod.sh`). **바꾸는 곳이 다섯 곳이 됐다** |
| 타일뷰 기본 정렬 | 들어올 때마다 `group=true` · `sortMode='date'` · `dateOrder='recent'` 로 되돌린다 |
| 모음 검색 | '현재 말씀 모음' 검색에 **태그**도 포함 |
| 카드 헤더 −/+ | 글자 크기를 설정 팝업 없이 한 칸씩 (`vcStepTextScale`). 일반카드는 맨 우측, 목록카드는 햄버거 **왼쪽**. 끝에 닿으면 그 버튼이 흐려지고 안 눌린다 |

#### 0811-1 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 제목 줄임 | '말씀카드 테마'·'말씀카드 글자 크기' → '테마'·'글자 크기'. 말씀설정 전체화면 탭도 같게 |
| 글자 크기 고르기 | 숫자 대신 **그 크기의 '태초에…'** 를 보여준다 (`.ts-btn`, `VC_TS_PX`). 작은 것부터 50·60·80·100 순서. '기본'(전체화면 따라가기)은 없앴다 — 늘 네 단계 중 하나를 갖는다 |
| 새 카드 기본값 | 테마=전체화면 따름 · 글자 크기=`VC_TS_DEFAULT`(0.8) · 좌·우 하단 **둘 다 꺼짐** |
| ⚠️ 전체 스위치는 **상위 개념** | `cfg.showGroup{meta,react}` 로 따로 둔다. 개별 값(`cfg.show`)은 **건드리지 않는다** — 껐다 켜면 하나하나 정해 둔 값이 그대로 되살아난다. `_vcShowFor` = 구역이 켜져 있고 **그리고** 개별이 켜져 있을 때만 보임. 구역이 꺼져 있으면 아래 줄들은 흐리게 보여 "값은 살아 있다"를 알린다 |
| 옛 카드 이어받기 | 0811-1 이전 카드에는 전체 스위치 개념이 없었다 = 늘 켜져 있던 셈 → `_vcGet` 에서 `{meta:true,react:true}` 로 옮긴다. 그래야 지금 보이는 대로 유지된다 |
| 반응 줄 아이콘 | `_VLIST_KIND_TITLE` 의 그림을 그대로 쓴다. ⚠️ `_VC_ACT_SVG` 는 아웃라인·채움 **두 벌**이라 그대로 못 쓴다 (문자열을 잘라 쓰려다 `tools/verify.py` 의 svg 균형 검사에 걸렸다 — 정규식 안의 `<svg` 도 세어진다) |

#### 0810-2 에서 손본 것 (HB 피드백)

| 무엇 | 어떻게 |
|---|---|
| 톱니가 해처럼 보임 | 점 세 개(`_VC_ICON_MORE`)로 |
| 목록 헤더 카드 버튼 | 빈 정사각형만 (안의 두 줄 삭제) |
| 손가락에서 버튼이 파랗게 굳음 | `:hover` 를 `@media (hover:hover)` 안으로. 마우스 없는 기기는 hover 가 풀릴 일이 없어 누른 색이 그대로 남았다 |
| 카드 갈래(필터) | `cfg.filter={kind,val}`. 좌하단·장절을 눌러 들어간 타일뷰에서 말씀을 고르면 (`_vgCardTarget` → `vgPick`) 전체화면이 아니라 **그 카드**에 뜨고, 그때부터 카드가 그 갈래 안에서 돈다. 헤더의 갈래 이름을 누르면 풀린다 |
| ⚠️ 카드끼리 서로 넘어감 | 자리를 "몇 칸 옮겼는지(off)"가 아니라 **장절(`cfg.ref`)** 로 들고 있게 바꿨다. off 는 기준(오늘의 구절)을 여러 카드가 나눠 써서 한 장을 넘기면 다른 장도 따라 움직였다. `_lay()` 도 **같은 카드 id 가 두 번** 들어가는 것을 막는다 |
| ⚠️ 장절을 눌렀는데 엉뚱한 성경 | `.vc-body` 제스처에 `active` 를 뒀다. 예전엔 버튼·장절이면 touchstart 만 건너뛰고 touchend 는 그대로 돌아서 **지난번 손가락 위치**로 이동거리를 쟀다 → "밀었다"고 보고 말씀이 넘어간 뒤, 곧이어 실행된 장절 누름이 넘어간 말씀으로 걸러 줬다 |
| ⚠️ 전체화면 장절이 안 눌림 | 화면 아래 절반을 '다음 말씀' 버튼(`.vf-nav-d`, z-index 2)이 덮는다. 본문이 길어 장절이 가운데 아래로 내려가면 눌림을 뺏겼다 → `#vfRef` 에 `z-index:3` |
| 반응 토스트가 메뉴에서만 옛 문구 | `_reactWithToast(kind,ref)` 한 곳으로 모았다 (전체화면·카드·말씀 메뉴 두 종류). 유지 시간 3.5초 → 2.5초 |
| PC 좌우 넘김 | `.vc-arrow` (마우스 기기에서만, 평소 흐리게·호버에 진하게). 화살표 자리만큼 `.vc-inner` 좌우 여백을 넓혀 글자와 겹치지 않게 했다 |
| 카드별 좌·우 하단 켜고 끄기 | `cfg.show{}` + `_vcShowFor(cfg,key)` (없으면 말씀설정 '뷰' 탭을 따름). 설정 팝업 순서는 테마 · 글자 크기 · 왼쪽 아래 · 오른쪽 아래 · 안내 |
| 공유 알약 | 말씀설정 뷰 탭 우하단에 '공유' 추가 (`verseCardShare`) |

#### 0810-3 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 스와이프가 손끝을 따라온다 | 카드 안에 `.vc-slide` 층을 하나 두고 그것만 움직인다 (배경은 제자리). 미는 동안 `paint(dx*0.85)` 로 칠하고, 44px 넘게 밀면 `_vcSlideCommit` 이 **밀던 자리에서 이어서** 밀어낸 뒤 반대편에서 들여보낸다. 덜 밀면 `snapBack`. 방향(`axis`)은 제스처마다 한 번만 정해 가로·세로가 싸우지 않게 한다 |
| + 버튼 | 작은 메뉴를 없애고 **바로 위젯 설정 팝업**을 연다 (`rpAddMenu` 삭제) |
| '말씀카드' 만들기 칩 | `VC_NEW='card#+'` — 설정이 없는 id 라 실수로 `cols` 에 남아도 `_lay()` 가 지운다. 끌어다 놓으면 `vcAddCard(col,idx)` 가 진짜 카드를 만들고, 칩은 '사용 안 함' 맨 앞 줄에 **그대로 다시 나타난다** (개수 제한 없음) |
| '사용 안 함' 세 줄 | `poolRows` = [말씀카드] / [주간뷰·월간 싱글·월간 트리플] / [좋아요·암송·Deeper·Even]. 칩 이름은 `_rpChipName` 이 '목록'을 뗀다 |
| ⚠️ 클래스 이름 충돌 | 줄 클래스를 `.rp-cfg-row` 로 지었다가 **이미 있던 같은 이름**(테두리·여백 붙는 설정 행)에 걸려 줄마다 28px 씩 벌어졌다 → `.rp-pool-row` 로 바꿨다. 새 클래스를 지을 때는 `grep` 으로 먼저 확인할 것 |

#### 0810-4 에서 손본 것

| 무엇 | 어떻게 |
|---|---|
| 말씀설정 뷰탭의 '말씀카드에서 보여줄 항목' | 통째로 제거. 카드마다 따로 있어서 겹친다. `_settingsDefaults` 의 `verseCard*` 는 **카드가 따로 안 정했을 때의 기본값**으로 그대로 남는다 (`_vcShowFor`) |
| '갈래' → '필터' | 사용자에게 보이는 말을 전부 '필터'로 통일 |
| 구역 전체 스위치 | `swTitle(label,which)` + `setVcShowAll(id,which,on)` · 구역 목록은 `_VC_SHOW_GROUP{meta,react}`. 다 켜져 있을 때만 켜짐으로 보이고, 누르면 그 구역을 통째로 켜거나 끈다 |
| ⚠️ 카드 밖에서 놓으면 튕겼다 사라짐 | 넘김 목적지를 **화면폭의 40% 로 고정**해 뒀는데, 손가락이 그보다 더 밀고 나가 놓으면 목적지가 **뒤에** 있어 되돌아왔다. `_vcCurX()` 로 지금 칠해진 자리를 읽어 **늘 더 바깥**을 목적지로 잡는다 |
| ⚠️ PC 에서 헤더 필터 이름 클릭이 죽음 | 헤더(`.rp-widget-title`)는 위젯 순서 이동 손잡이라 `mousedown` 에서 `preventDefault` 를 한다. 손잡이는 `e.target.closest('button')` 만 비켜 주므로 **span 이면 클릭이 통째로 죽는다** → `.vc-filter` 를 button 으로. 헤더에 새 요소를 놓을 땐 반드시 button 으로 둘 것 |
| ⚠️ 필터 = **교집합** | 필터는 원래 범위를 **덜어내는** 것이지 갈아치우는 것이 아니다. 목록카드에 필터가 걸리면 `그 반응 목록 ∩ 필터`. 타일뷰도 같은 교집합을 보여야(`_vgState.limitKind`) 거기서 고른 말씀이 카드로 돌아갔을 때 사라지지 않는다 |

설계와 다르게 간 세 곳 (HB 확인 필요):
- **카드 헤더 좌상단에 톱니를 하나 더 뒀다.** 설계서의 진입 ①(헤더 여백 롱터치)은
  터치에서 위젯 순서 이동(300ms 홀드)과 겹친다. 그래서 우클릭(PC)은 그대로 두고,
  터치에서도 늘 되는 자리로 톱니를 뒀다. 일반카드위젯은 이게 없으면 설정에 들어갈
  길이 아예 없다. (헤더는 두 종류 다 있고, 일반카드는 중앙·우측이 비어 있다)
- **전체화면 '목록 복귀'는 우상단이 아니라 좌상단.** 우상단은 × 자리라
  (닫기는 × 하나로 통일) 좌상단 보조 버튼 자리에 햄버거를 뒀다. × 로 닫아도
  숨겨 둔 목록이 되살아나므로 결과는 같다.
- **우하단 알약은 네 개(좋아요·암송·Deeper·Even)뿐이고 공유는 늘 보인다.**
  설계서가 네 개만 적어 두어 그대로 따랐다.

### ✅ 해결됨 (참고용 이력)

**5-1. 여러 기기 동기화가 옛 상태로 덮어씀** — v26-0731-5에서 rev 트랜잭션 +
3자 병합 + 자동 백업으로 해결. v26-0802-1에서 구버전·유휴 기기 구멍까지 막음.
자세한 구조는 **7장** 참조. 이 영역을 건드릴 때는 7장을 먼저 다 읽을 것.

**5-2. 같은 구절이 약칭본·정식본 두 개로 존재** — v26-0731-6에서
**중복 구절 정리 도구**로 해결 (계정 탭 → 데이터 복구 → "중복 구절 정리").
`_dupVerseScan` / `mergeDuplicateVerses` / `_rewriteLogRefs`.
같은 모음 안에서 **정규화 장절 + 대분류 + 소주제**가 같을 때만 합친다.
남기는 우선순위: 구글 시트 유래 > 날짜(d) 있음 > 정식 표기 > 먼저 있던 것.
로그 ref 이관은 **흡수된 표기가 다른 모음·기본180에 남아 있지 않을 때만**.

**5-3. '기준날짜 / 만 기간' 버튼이 둘 다 비선택으로 보임** — 5-1 수정 후
증상 사라짐(HB 확인). 원인은 옛 기기가 `periodBaseMode`를 되돌리던 것.

### 5-3. 첫 화면이 늦게 펼쳐지는 문제 — **해결** (v26-0803-2)
- **증상**: PC 크롬에서 페이지를 처음 열 때 GNB·달성바만 나오고 본문이 한참 뒤에 나타남.
  모바일에서도 가끔.
- **원인**: 로그인 사용자는 **클라우드 최초 fetch 를 await 한 뒤에야** `initAppUI()` 가
  돌면서 본문이 그려진다. 네트워크가 느리면 그동안 정적 HTML(GNB·달성바)만 보인다.
- **고치려면**: "로컬(localStorage)로 먼저 그리고, 클라우드가 오면 갱신"으로 부팅 순서를
  바꿔야 한다.
- ⚠️ **위험**: `initAppUI()` 안에는 `runAutoCarryOver()` · `ensureDailyRepeats()` 등
  **쓰기 동작**이 함께 돈다. 클라우드 도착 전에 이것들이 돌면 7장이 경고하는
  덮어쓰기 사고가 난다. **그리기(읽기)와 쓰기를 분리한 뒤** 손대야 한다.
- **해결 방법** (v26-0803-2): `paintAppUIFromLocal()` — 직전 세션과 같은 계정인
  기기(`_localOwner` + 로컬 데이터 있음)는 SDK 로드·인증·fetch 를 기다리지 않고
  localStorage 의 ST 로 즉시 그린다. 클라우드가 도착하면 기존 병합·적용 경로가
  다시 그린다(재렌더는 initAppUI 가 담당 — applyRemoteState 는 화면을 안 그림).
  - **그리기만** 한다. 쓰기(이월·반복·알람·시트)는 여전히 fetch 후 initAppUI 에서만.
    ⚠️ 쓰기를 fetch 앞으로 옮기지 말 것 — 7장의 덮어쓰기 사고가 재발한다.
  - 그리는 동안의 편집은 부팅 3자 병합이 살린다 — 전제는 `tests/test_boot.js` 가 고정.
    같은 날짜 동시 편집은 날짜 단위로 로컬이 이긴다(기존 병합 규칙, 오프라인 편집과 동일).
  - 리스너 등록은 하나도 안 한다(전부 initAppUI 몫 — 두 번 등록 방지).
  - 로그인 막(`#authScreen`)도 미리 걷는다. 세션이 정말 만료됐으면
    `onAuthStateChanged(null)` 이 다시 띄운다.

### 5-4. 대시보드 미확정 사항 (HB 확인 대기)

HB가 기기에서 직접 보고 판단할 항목. 답이 오면 바로 반영하면 된다.

| # | 항목 | Claude 의견 |
|---|---|---|
| a | 파이 조각 이름 표시 기준(8% 미만 생략) | 아이폰 세로에서 겹치면 10%로 상향 |
| b | 태그·성경 상위 7개 + '기타' 묶기 개수 | '기타'가 절반 넘는 축이 있으면 10개로 |
| c | 아이폰 세로 16칸 표의 좌우 스크롤 | 답답하면 세로일 때 2열×8행 재배치 (중간 규모 변경 → 먼저 제안) |
| d | 옛 '암송 이력' 화면 (진입로 없는 죽은 코드) | **삭제 권장** — 대시보드가 대체함. HB 승인 후 정리 |

### 5-5. 앞으로 할 만한 것 (우선순위 낮음)

- 기기별 알림 on/off UI (현재 알림 설정은 계정 동기화라 한 기기에서 끄면 전부 꺼짐)
- 검증 스크립트를 저장소에 상주시키기 (`tools/verify.py`)
- 병합 테스트를 저장소에 상주시키기 (`tests/`)

---

## 6. 핵심 아키텍처

### 말씀 반응 (좋아요 / 암송 / Deeper / Even / 공유)
- 로그: `ST.verseLikeLog` / `getMemLog()` / `getDeeperLog()` /
  `ST.verseEvenDeeperLog` / `ST.verseShareLog`
- 기록 진입점: `recordVerseLike` / `recordMemorizationByRef` /
  `openDeeperFromRef` / `openEvenDeeperFromRef` / `recordVerseShare`
- **로그는 누른 시점의 장절 문자열을 그대로 저장한다** → 나중에 모음의
  표기가 바뀌면 못 찾는다. 대조할 땐 `_findVerseByRefLoose()` 를 쓸 것
- **소속 구간은 기록 시각으로 정한다** — `_entrySecId(en)` = `en.sec || _secIdForTime(en.time)`
- 전체 사용자 합계: `verseStats/{ref}` 문서에 `increment(1)`
- 실시간 갱신: `refreshVerseMarksLive()` / `refreshTaskViewsLive()`
  ⚠️ `renderRightPanel()`은 컬럼·위젯만 그림 → `renderToday()`도 같이 호출

### 말씀 대시보드 (v26-0731-4 신규)
- 진입: 로고 메뉴 → **대시보드** (파이차트 아이콘, '현재 말씀 모음' 아래)
- **4행(좋아요·암송·Deeper·Even) × 4열(대분류·소주제·태그·성경) = 16 파이차트**
- 데이터는 말씀 목록과 같은 경로 재사용: `_flatSimpleEntries` / `_flatMemEntries`
  / `_aggByRef` / `_vListRange` → **기간 기준(periodBaseMode)을 자동으로 따름**
- 기간 필터는 글자 버튼 5종(주간·월간·연간·전체·직접), `ST.settings.vDashPref`에 저장
- 조각은 **상위 7개 + '기타'**. '기타'는 묶음이라 필터로 못 여니 상세 팝업으로 보냄
- 셀: 파이 누르면 상세 팝업 / 1등 항목 누르면 타일뷰 / 1등 장절 누르면 전체화면
- 나갔다 돌아오면 대시보드(+열려 있던 상세 팝업)가 복원됨 — `_vDashReturn`,
  `closeVerseGrid`·`closeVerseFull` 에 훅이 걸려 있음

### 말씀 전체화면 (릴스형)
- 진입: 말씀영역 롱터치 메뉴 / 더블탭 / 위젯 메뉴 / 알림 / 대시보드
- **손가락 추종 세로 드래그**, 70px 넘으면 커밋(`_vfNavCommit`)
- **더블탭 = 좋아요**, **왼쪽 가장자리→오른쪽 스와이프 = 닫기**
- ⚠️ `openVerseFull(keepOverride)` — **인자 없이 부르면 `_vfOverrideVerse`를 지운다.**
  특정 구절을 지정해 열 때는 반드시 `openVerseFull(true)` 로 부를 것
  (알림·구절메뉴·대시보드에서 엉뚱한 구절이 열리던 버그의 원인이었음)

### 테마 시스템 (전체화면·타일뷰·공유카드 공용)
- `VF_PATTERNS` = **패턴 8종 × 색 변형 9종 = 72 프리셋**
- **단일 소스 원칙**: CSS 변수와 캔버스가 같은 객체를 읽음
- 타일은 `tileDesat`으로 **채도만** 낮춤 (배경색 쪽으로 섞으면 밝은 패턴이 흰색으로 뭉개짐)
- 명조 선택 시에만 Google Fonts 지연 로딩 → **`document.fonts.ready` 후 `_vfLayoutText()` 재계산 필수**

### 타일 그리드 (`#verseGrid`)
- `openVerseGrid(kind,val)` — kind는 `cat|topic|tag|book|home`
- 전체화면이 안 떠 있어도 동작함 (`vgPick`이 알아서 전체화면을 띄움)
- 행 우선 배치: `.vg-group > .vg-col × n`, `i % cols` 로 분배
  ⚠️ CSS 멀티컬럼 + `column-span:all` 은 그룹 경계에서 깨짐
- 진입 시 보던 구절 타일에 **숨쉬는 글로우 3초 → 3초 페이드**

### 공유
- **화면을 실측해 캔버스에 옮겨 그린다** (`_shotDraw`)
- 아이콘은 `.vf-ic` 컨테이너를 재고 채움 여부는 `.on` 클래스로 판정
- 데스크탑은 공유 시트 대신 바로 다운로드 + 클립보드

### 한국어 줄바꿈 엔진 (`_vfWrapFit`)
- **DP(최소비용)** 방식. 한 줄 4~8단어
- 캔버스로 실제 렌더 폭 측정. **폭은 컨테이너에서 잰다**
  (`#vfText`에서 재면 shrink-to-fit 되먹임으로 글자가 계속 작아짐)

### 말씀 푸시 알림
- 설정: `ST.settings.versePush = {enabled, order, timesOn, times[], interval, days[]}`
- **말씀 목록은 앱이 계산해서 저장** — `ST.settings.versePushPool`
- **할일 알림과 말씀 알림은 완전히 독립된 스위치** (v0730-4)
  - 공용 배관: `_ensurePushToken()` (등록) / `_releasePushTokenIfIdle()` (해제)
  - **둘 다 꺼졌을 때만** 기기 등록을 해제한다
  - 켤 때는 권한이 이미 있어도 **반드시 토큰까지 확인** (건너뛰면 푸시가 안 옴)
- **여러 기기 동시 수신 지원** (v0730-11) — `devId`(localStorage `b7v1_devid`)로
  기기를 구분해 **내 옛 등록만** 지운다. 아이폰·아이패드·맥 크롬 동시 가능
  - ⚠️ 알림 켜기/끄기 **설정은 계정에 동기화**되므로 한 기기에서 끄면 전부 꺼진다.
    기기별로 끄는 UI는 아직 없음 (필요하면 기기 목록 UI를 만들 것)
- 알림 클릭 → 그 말씀 전체화면. **세 갈래로 받음**:
  ① 주소 `?verse=` ② 서비스워커 메시지/브로드캐스트 ③ 캐시에 남긴 기록 + 폴링
  - ⚠️ iOS PWA는 알림에서 열 때 주소를 무시함 → ③이 핵심
  - 앞으로 나올 때 **0.25초 간격 5회 재확인**(`recheckBurst`) — 워커가 기록을
    저장하는 순간과 앱이 앞으로 나오는 순간이 경쟁하기 때문
  - 전달용 기록은 **전체화면이 실제로 뜬 뒤에만** 지운다
  - ⚠️⚠️ **v0803-5 전면 재작성** — 아래 네 가지가 실제 실패 원인이었다. 되돌리지 말 것:
    1. **장절 기반 5초 중복막기**(`_lastNotifRef`) → 같은 말씀 알림을 다시 눌러도 안 열렸다
    2. **250ms 뒤 한 번만 시도** → 그 순간 앱이 뒤에 있거나 준비 전이면 영영 실패
    3. **"전체화면이 떠 있으면" 성공으로 보고 기록 삭제** → 이미 *다른* 말씀이 떠 있으면
       새 말씀은 안 떴는데 기록만 사라져 재시도 기회까지 잃었다
       (8-4-2 → 8-4-3 → 8-4-5 로 이어지던 경로의 정체)
    4. **`setVerseIdx()` 로 열기** → `verseCurrentIdx` 는 계정 동기화 대상이라
       리스너가 클라우드 값으로 덮어써 원래 말씀으로 되돌아갔다
    지금 방식: **알림 1건 = 토큰(기록의 ts) 1개**로 중복을 막고, 목표 말씀은
    **`_vfOverrideVerse` 로 고정**(동기화가 못 건드림), 연 뒤 **정말 그 말씀이 떠 있는지
    확인될 때까지 재시도**하고, **확인된 뒤에만** 기록을 지운다. `tests/test_notif.js` 27건.
  - ⚠️ **말씀 목록에서 못 찾아도 한 번에 포기하지 말 것** (v0803-6). 알림으로 앱이
    "막 켜지는" 중이면 클라우드에서 말씀을 받기 전이라 목록이 비어 있다. 한 번에
    포기해서 "종료 상태에서 알림 누르면 앱만 뜨던" 문제가 있었다. 재시도 차례를
    다 쓴 뒤에만 포기한다
  - ⚠️ **`notificationclick` 에서 `client.navigate()` 를 쓰지 말 것** (v0803-6에서 제거).
    페이지가 통째로 새로고침돼 ① 알림 전체화면을 닫았을 때 보던 화면 복원이
    무력화되고 ② 매번 앱이 다시 뜬다. 메시지가 유실돼도 폴링+재시도가 덮는다
  - ⚠️⚠️ **아이폰 홈화면 앱은 캐시 저장소(caches)에 쓴 것이 남지 않는다.**
    **전달용 기록(`__pending_verse`)이 바로 그 캐시에 있었다** — "아이폰은 이 경로가
    핵심"이라고 이 문서에 적어 둔 그 기록이 정작 아이폰에서 불안정했다.
    v0803-8부터 **IndexedDB(`block7`/`kv`)가 주 저장소**, 캐시는 구버전 호환용 보조다.
    진단 기록도 같다(워커가 IndexedDB 에도 남김 → 알림 도착 시점 기록이 아이폰에서
    사라지지 않는다). **캐시만 쓰도록 되돌리지 말 것.**
  - ⚠️ **아이폰 홈화면 앱은 캐시(caches)에 쓴 진단 기록이 남지 않는다** (v0803-7에서 대응).
    "알림도 오고 전체화면도 뜨는데 진단 기록만 계속 비어 있던" 원인. 이제 앱 기록은
    localStorage(`b7v1_notiflog`)에 먼저 쓰고, 서비스워커도 열린 창에 같은 줄을
    보내 앱이 받아 적는다. 보기는 캐시+localStorage 를 합쳐 시간순으로 보여준다.
    ⚠️ swLog 가 창에 보내는 줄과 캐시에 넣는 줄은 **같은 객체**여야 한다(시각이
    달라지면 합칠 때 같은 줄이 두 번 보인다)
  - ⚠️ **진단 기록은 운영본과 개발본이 같은 캐시를 공유한다.** 개발본 탭이 열려 있으면
    그 탭 기록도 섞여 들어와 오해하기 쉽다(개발본은 말씀 목록이 달라 '못 찾아 못 엶'이
    뜬다). v0803-6부터 앱 기록에 `(개발본)` 표시가 붙는다
  - ⚠️ 1.5초 폴링에 `visibilityState==='visible'` 조건을 **다시 걸지 말 것** (v0803-1).
    iOS PWA 는 오래 잠들었다 깨면 visibilitychange 가 안 오거나 visibilityState 가
    'hidden' 에 멈춰 있어, 그 조건이 폴링을 영영 죽였다. "오랜만에 열면 알림을
    눌러도 앱만 뜨는" 재발의 원인. 지금은 타이머 공백(>5초)으로 깨어남을 감지해
    recheckBurst 를 돌린다. 그래도 재발하면 진단 기록에서 "[워커] 알림 탭됨" 이
    그 시각에 있는지부터 볼 것 — 없으면 워커 클릭 처리기 자체가 못 돈 것이다
  - 알림 전체화면이 덮었던 화면(타일뷰·설정·대시보드)은 **닫지 말고 숨겼다 복원**
    (`_vfNotifHideCovers`/`_vfNotifRestore`, closeVerseFull 이 복원) — 닫아버리면
    전체화면을 닫았을 때 보던 화면이 사라져 있다 (v0803-1)

### 말씀 푸시 발송 서버 (`versepush/`)
**Cloud Run 서비스**로 따로 배포된다 (Firebase Functions 아님 — 콘솔에 "Cloud Run"으로 표시).
- 소스: 저장소의 **`versepush/`** (2026-08-03에 Cloud Run 콘솔에서 받아와 저장소에 넣었다)
- 배포: Cloud Run 콘솔 → `versepush` → **소스 → 소스 수정** → 붙여넣고 **저장 후 다시 배포**
  (`gcloud run deploy versepush --source versepush/ --region asia-northeast3` 도 가능)
- 저장소의 `functions/index.js` 는 **할일 알림**과 테스트 발송만 담당한다 (별개)
- ⚠️ **웹 푸시라서 `apns` 가 아니라 `webpush.headers` 가 맞는 자리다.**
  아이폰 홈화면 앱·맥 크롬 모두 웹 푸시 토큰을 쓴다.
  `Urgency:'high'` 가 없으면 FCM 이 normal 로 보내고 애플·구글이 배달을 미뤄
  5~8분, 심하면 1시간 늦게 도착한다. `TTL` 로 늦어진 알림은 버려지게 한다
- ⚠️ 암송 기록 키는 **`memorizationLog`** 다. 예전엔 `verseMemLog` 로 되어 있어
  **'암송순'이 한 번도 동작하지 않았다** (전부 0 → 목록 첫 구절만 계속 선택)

### 서비스워커 (`firebase-messaging-sw.js`) — 순서가 곧 기능
```
① notificationclick 등록  ← importScripts 보다 반드시 "먼저"
② install / activate / push
③ importScripts(firebase) + firebase.messaging()  ← 맨 마지막
```
- FCM은 자기 클릭 처리기에서 `stopImmediatePropagation()`으로 뒤에 등록된
  처리기를 전부 막는다 → 우리가 먼저 잡고 우리도 막는다
- `importScripts`는 gstatic에서 파일을 받아온다. 네트워크가 나쁘면 **거기서
  스크립트 실행이 통째로 멈춰** 뒤의 등록이 하나도 안 된다
  → "한동안 되다가 어느 순간부터 알림 눌러도 전체화면이 안 뜨던" 원인
- **push 도착 시 알림 표시를 우리가 직접 한다** (v0803-5). 예전엔 표시를 FCM 에
  맡겼는데, push 를 받고도 알림을 안 띄우면 크롬이 **"이 사이트는 백그라운드에서
  업데이트되었습니다"** 가짜 알림을 대신 띄운다. importScripts 가 막히면 FCM 처리기가
  아예 등록되지 않아 실제로 그 일이 벌어졌다. 이제 `push` 에서
  `stopImmediatePropagation()` + `showNotification()` → 중복도 없고 가짜 알림도 없다
- **앱이 화면에 떠 있을 때(foreground)는 브라우저가 알림을 자동으로 안 띄운다.**
  데스크탑 크롬에서 탭을 열어두면 "알림이 아예 안 오는" 것처럼 보였다 → 앱의
  `onMessage` 에서 직접 `showNotification` 한다. 옛 코드는 `payload.data` 를 먼저 읽어
  (말씀 푸시의 data 엔 title/body 가 없다) 토스트에 **"알림"** 만 떴다
- ⚠️⚠️ **클릭 처리에서 "저장을 기다린 뒤" 다른 일을 하지 말 것** (v0803-9).
  예전 규칙은 "전달용 기록 저장이 제일 먼저"였다. 저장이 빠르다는 전제였는데,
  **iOS 는 페이지가 같은 IndexedDB 를 쥐고 있으면 워커 쪽 open 이 멈춘다.**
  그러면 `waitUntil` 이 안 끝나고 뒤에 매달린 "창에 알리기 · 창 앞으로 · 진단 기록"이
  **하나도 실행되지 않는다** → 알림을 눌렀는데 아무 일도 없고 기록조차 안 남는다.
  (HB가 19:41에 겪은 것 — `push 도착`은 있는데 `알림 탭됨`이 없던 이유)
  지금은 저장을 `withTimeout` 으로 **따로** 돌리고 창에 알리는 일을 즉시 한다.
  **모든 저장소 호출(워커·앱 양쪽)에 시간 제한을 유지할 것.**
- 진단 로그는 맨 뒤 (iOS 는 워커에 짧은 실행 시간만 준다)
  (iOS는 워커에 짧은 실행 시간만 준다)
- **알림 진단 기록**: 말씀 설정 → 알림 탭 → "알림 진단 기록" (보기/복사).
  워커와 앱이 캐시 `block7-msg` 의 `/__notif_log` 를 공유

### 구글 시트 동기화
- 짝짓기 기준 = **장절 + 대분류 + 소주제** (`_verseIdentity`)
- **이어받기(fallback)는 "시트에도 한 번, 앱에도 한 번"일 때만** (v0730-14)
  - ⚠️ 예전엔 장절만 같으면 아무거나 집어와 소주제를 덮어썼다.
    같은 구절이 여러 소주제에 쓰이는 시트에서 소주제가 뒤섞였음
- ⚠️⚠️ **2026-08-02 대량 삭제 사고 — 반드시 읽을 것**
  - **증상**: 로고 롱터치로 동기화하니 150구절 중 **145개가 한 번에 휴지통**으로
    갔고, 다시 동기화해도 계속 휴지통 상태로 유지됐다. 남은 6개는
    "머리글이 구절로 등록된 유령 1개 + 그날 추가한 5개"였다.
  - **원인**: `gviz/tq?tqx=out:csv` 는 열의 자료형을 스스로 추정하면서
    **앞쪽 행들을 통째로 머리글로 합쳐버리는** 경우가 있다. 그래서 150행
    시트가 "머리글 1행 + 데이터 5행"으로 내려왔다. CSV 파서도 시트 파일도
    멀쩡했다 — **구글이 준 응답 자체가 잘린 것**이었다.
    앱은 이걸 "시트에서 145개가 지워졌다"고 해석했다. 삭제 조건이
    `items.length` 가 0이 아닌지만 보고 있었기 때문에 5개로도 통과했다.
  - **고친 것 (v26-0802-2)**
    1. `_fetchSheetCsv` 엔드포인트 **순서 교체** — `export?format=csv` 가 1순위.
       이건 시트를 있는 그대로 준다. gviz 는 폴백이며 `headers=1` 을 붙였다.
       BOM도 제거한다.
    2. `_sheetRowsSane(rows)` — 앞 3행 셀에 **줄바꿈이 들어 있으면**(뭉친 응답의
       징후) 통째로 거부하고 **아무것도 건드리지 않는다.**
    3. `_looksLikeRef(ref)` — 장절답지 않은 행은 버리고, 이미 들어온 유령
       구절(구글 유래인데 장절이 이상한 것)은 동기화 때 자동 제거.
    4. **대량 삭제 안전장치** — 지우기 *전에* 규모를 본다. 이 시트가 관리하던
       것의 30%를 넘고 5개를 넘게 지우려 하거나(`tooMany`), 받아온 행 수가
       원래의 절반도 안 되면(`tooFew`) **정리를 보류**하고 `blocked` 로 돌려준다.
       수동 가져오기에서는 `appConfirm` 으로 사람에게 묻고, 승인하면
       `srcInfo.force=true` 로 진행한다. 전체 업데이트·자동 동기화는 보류만 하고 알린다.
    5. 헤더 판정을 정확히 일치 → **포함 검사**로 강화.
  - **교훈**: *"받아온 게 비어 있지 않다"는 것은 안전 조건이 못 된다.* 외부에서
    받아온 데이터로 **지우는** 동작은 반드시 **규모**를 함께 봐야 한다.
    테스트는 `tests/test_sheet.js` (23건, 사고 재현 포함).
- **시트에서 사라진 구절은 휴지통행(`del:'simple'`)** (v0730-14)
  - 예전엔 지우는 절차가 아예 없어 유령 항목이 쌓였다(실제 18개 정리됨)
  - 시트에 다시 생기면 자동 복구. 받아온 내용이 비면 아무것도 건드리지 않음
  - 구글 소스마다 `gid`(`_gSrcId`)를 붙여 다른 시트 것은 건드리지 않음
- 로고 **탭 = 말씀 메뉴**, **롱터치/우클릭 = 전체 업데이트**(`verseSyncAllNow`)
- **중복 구절 정리 도구** (v26-0731-6) — 계정 탭 → 데이터 복구 →
  "중복 구절 정리". `_dupVerseScan`/`mergeDuplicateVerses`/`_rewriteLogRefs`.
  같은 모음 안에서 **정규화 장절+대분류+소주제**가 같을 때만 합친다.
  ⚠️ `_refNorm()`은 **공백까지 제거**한다 → 정식 표기 판정 시
  `_refNorm(ref) === ref.replace(/\s+/g,'')` 로 비교할 것 (한 번 틀렸던 지점)
- ⚠️ **말씀 모음 칩의 개수는 기간 필터를 통과한 것만 센다**(`_collPeriodPass`).
  시트 총 개수와 비교할 땐 기간을 '전체'로 두고 볼 것
- ⚠️ 구글 시트 찾기로 개수를 세면 **다른 열에 같은 글자가 있으면 중복으로 잡힌다**
  (예: '2일' 검색 → 주제 26 + 설교제목 '리트릿 2일차 저녁' 21 = 47)

### 레이아웃 (1/2/3단)
- `_layMode()` — 폭 기준(bp2=600, bp3=900) + **폰 세로면 무조건 1단**
- ⚠️ `screen.width` 는 안드로이드에서 **물리 픽셀**을 돌려줌 →
  뷰포트 기준 폭은 `documentElement.clientWidth` (`_deviceBaseW()`)
- 설정 위치: **뷰 탭 맨 아래** '레이아웃 분할 기준' (v0731-1에 일반→뷰로 이동)

### 설정창 구조
- **일반 설정** 6탭: `일반 · 알림 · 뷰 · 버튼 · 구간 · 계정`
  - 탭 슬라이드 폭은 `SETTINGS_TABS` 길이에서 자동 계산(`--stab-count`).
    탭을 늘려도 안 깨짐
  - **관리자 전용**(`_syncDevInboxVisibility`): 알림 테스트, 데이터 복구, 받은 쪽지
- **말씀 설정** 5탭: `뷰 · 알림 · 전체화면 · 공유 · 말씀 모음`
  - 전체화면 카운터는 **전체화면 탭 맨 아래**
  - 말씀 인앱 팝업 기능은 **삭제됨**(v0731-1). `scheduleVerseAlarms()`가
    맨 앞에서 `return` — 계정에 남은 옛 설정값으로 팝업이 뜨지 않게 막음

### 앱 자체 확인창 · 토스트
- `appConfirm(msg, onYes)` — **네이티브 `confirm()`은 iOS PWA에서 조용히 무시됨**
- `showToast(msg)` — 4.5s + 0.5s 페이드
- ⚠️ **iOS 복사는 탭 직후 동기적으로 시작**해야 한다. `await` 뒤에 복사하면
  조용히 실패 → `ClipboardItem`에 Promise를 넣는 방식 사용

### 받은 쪽지 (개발자 전용)
- 읽음 표시는 **계정에 저장**(`ST.settings.devNotesRead`) — 기기 간 공유 (v0730-12)
  - 예전엔 localStorage에만 둬서 기기마다 따로였고, 도메인을 옮기니 전부 초기화됐음
  - 기기에 남은 옛 기록은 `_devMigrateRead()`가 최초 1회 계정으로 올림
- ⚠️ `orderBy('createdAt')` 쓰면 그 필드 없는 문서가 조용히 제외됨 → 클라이언트 정렬

### 링크 공유 카드 (Open Graph)
- `index.html` head에 `og:*` / `twitter:*` 태그, 이미지는 `og-image.png` (1200×630)
- ⚠️ `og:image`는 **전체 주소**여야 함. 상대경로는 무시됨
- ⚠️ 카카오톡은 카드를 오래 캐시한다. 내용을 바꾸면 `?v=` 숫자를 올리고
  https://developers.kakao.com/tool/debugger/sharing 에서 캐시 초기화
- 확인용 요령: `https://block7.my/?t=2` 처럼 뒤에 값을 붙이면 새로 긁어옴

---

## 7. 동기화 구조 (⚠️ 이 영역을 고치기 전에 전부 읽을 것)

가장 비싸게 배운 영역이다. 세 번의 수정을 거쳤고, 각 수정마다
"고쳤는데 또 났다"가 반복됐다. 구조를 모르고 건드리면 **데이터가 사라진다.**

### 7-1. 기본 뼈대 (v26-0731-5)

저장 위치는 `users/{uid}/data/state`, 내용은
`{ json:"상태 전체 문자열", rev:정수, dev:기기ID, appVer:버전문자열, updatedAt }`.

- **`rev`(개정 번호)가 핵심.** 쓰기는 `_fbCommit()`의 **트랜잭션으로만** 한다.
  클라우드 rev가 내가 마지막으로 본 `_fbLastSeenRev`보다 앞서면
  덮어쓰지 않고 **먼저 받아 병합**한 뒤 `rev+1`로 쓴다.
- **3자 병합** `_fbMerge(base, local, cloud, preferCloud)`
  - `base` = 마지막으로 클라우드와 일치했던 상태.
    메모리 `_fbBaseJson` + localStorage `b7v1_syncbase` / `b7v1_syncmeta`에 영속.
  - 자료형별 규칙:
    | 자료 | 병합 방식 |
    |---|---|
    | 좋아요·공유·Deeper·Even 로그 | 항목 개수 기반 **합집합** (충돌 없음) |
    | `memorizationLog` | 날짜→구간 2단 합집합 |
    | `days` | 날짜 → 구간(secId) **배열 단위** 3자 |
    | `settings` | **키 단위** 3자 |
    | `verseCollections`·`contacts` | **id 단위** 3자 |
    | 그 외 | 통째 3자 |
  - 삭제 규칙: base에 있었고 한쪽만 지웠고 다른 쪽은 base 그대로 → 삭제 확정.
    한쪽은 지우고 한쪽은 내용을 바꿈 → **바꾼 쪽 유지**(보존 우선).
- **자동 백업** — 병합이 일어나면 직전 클라우드 원본을
  `users/{uid}/data/backup_{0..2}`에 분 단위 순환 저장. 관리자 UI에서 복원 가능.
- **리스너 가드 제거** — 옛 코드의 `_fbWriteTimer` 대기 중 무시 /
  2.5초 내 편집 시 무시는 **진짜 원격 변경까지 버려서** 사고를 키웠다.
  지금은 `rev <= _fbLastSeenRev` 이면 무시(에코 판정)로 대체. **되살리지 말 것.**
- 트랜잭션은 **오프라인 큐에 안 들어간다** → 실패 시 15초 재시도 +
  `online` 이벤트 + `visibilitychange(visible)` + `_fbEnsureSync()`.
  localStorage에는 항상 최신 ST가 있으므로 유실은 없다.

### 7-2. 구버전 기기 방어 (v26-0802-1) ⚠️ 재발했던 지점

**증상** — 7-1을 적용한 뒤에도 "오래 전에 열어둔 데스크탑 브라우저의 데이터가
가끔 모바일을 덮어쓴다"가 계속됐다.

**원인 두 가지**

1. **rev 없는 문서를 "새 것"으로 믿었다.**
   오래 열어둔 브라우저 탭은 **새로고침하지 않는 한 옛 코드로 계속 돈다.**
   그 탭은 rev를 모르므로 `{json, updatedAt}`만 써서 **rev 필드를 지워버린다.**
   옛 리스너 코드는 `rev===null`을 "새 것"으로 간주해 **통째로 적용**했다.
   → 폰의 최신 작업이 전부 사라짐.

2. **유휴 기기가 충돌에서 이겼다.**
   `_mgWhole`이 "둘 다 바꿈 → 무조건 로컬 우선"이었다. 며칠째 열어만 둔
   데스크탑 탭도 날짜가 넘어가거나 달력을 훑기만 하면 `ST`가 조금씩 달라져
   저장을 트리거하는데, 그게 로컬 우선을 타서 **지금 실제로 쓰고 있는 폰을 밀어냈다.**

**고친 방식**

1. **rev 없는 문서 = 믿지 않는다.** 그 기기는 rev 체계를 모르므로
   우리가 rev를 쓰기 시작한 이후의 변경을 한 번도 본 적이 없다 = **정의상 낡았다.**
   원본을 백업한 뒤 `_fbMerge(null, ST, remote, false)` — 즉 **base 없음(합집합) +
   로컬 우선** 으로 복구하고, 곧바로 정상 rev로 다시 올린다.
   옛 기기가 **새로 넣은** 날짜·항목은 합집합이라 함께 살아남는다.
   `_fbWarnLegacyWriter()`가 토스트로 한 번만 알린다(매번 띄우면 쏟아짐).

   > ⚠️ 여기서 `_fbBaseObj()`를 base로 넘기면 **안 된다.** base는 rev N 시점이고
   > 옛 기기의 상태는 그보다 훨씬 이전에서 갈라졌다. base를 주면
   > "로컬은 base와 같음 → 클라우드 채택"이 되어 **덮어쓰기가 그대로 통과한다.**
   > 반드시 `null`을 넘겨 합집합 경로를 타게 할 것.

2. **손이 닿지 않은 기기는 충돌에서 양보한다.**
   `_fbDeviceIdle()` = 화면이 백그라운드이거나 **2분간 입력 없음**.
   유휴면 `_mgPreferCloud=true`가 되어 충돌 시 **클라우드(=상대 기기)를 채택**한다.
   판정은 `pointerdown/keydown/touchstart/wheel` 캡처로 갱신되는 `_fbLastTouchTs`.
   충돌이 아닌 변경(내 고유 신규 날짜 등)은 유휴여도 **그대로 보존**된다.

3. **낡은 탭 자가 갱신** — 커밋 시 `appVer:APP_VERSION`을 함께 기록하고,
   리스너에서 `_fbMaybeSelfUpdate()`가 클라우드 버전이 더 새로우면
   **유휴일 때만** 1.5초 뒤 `location.reload()`. 사람이 쓰는 중이면 방해하지 않는다.
   이건 **앞으로의 재발 예방**이고, 이미 열려 있는 옛 탭에는 효과가 없다
   (그 탭엔 이 코드가 없으므로) → HB에게 그 탭을 닫으라고 안내해야 한다.

### 7-2-2. 낡은 버전이 "모르는 것"을 지우는 사고 (v26-0810-5) ⚠️ 세 번째 재발

**증상** — 집 맥미니 크롬에 **옛 버전으로** 하루 종일 열려 있던 창을,
다른 페이지에 밀려 있던 것을 **눌러서 앞으로 불러오기만** 했는데
사무실 맥·아이패드에서 만들어 둔 말씀카드가 **모든 기기에서** 사라졌다.
사람은 아무것도 바꾸지 않았다.

**원인 — 병합 엔진의 잘못이 아니다.**
옛 버전의 `_lay()` 는 `cols` 안의 `card#…` 를 "정의에 없는 타입"으로 보고
**조용히 지웠다.** 화면을 한 번 그리기만 해도 그 상태가 저장·전송됐고,
3자 병합은 그것을 **사람이 지운 것**으로 정확하게 처리해(로컬만 바뀜 → 로컬 채택)
모든 기기에 퍼뜨렸다. 즉 7-1·7-2 의 방어는 정상 작동했고, 방어 **바깥**에서
데이터가 파괴된 것이다.

> 실제로 확인한 것: 0807-4 의 `_lay()` 에 `{center:['card#c1'],right:['likeList','card#c2']}`
> 를 넣으면 `{center:[],right:['likeList']}` 가 나온다.

**고친 방식 (다섯 가지)**

1. **모르는 위젯은 지우지 않는다.** `_lay()` 가 아는 타입인지 `_layIsKnownType()`
   으로 보고, 모르는 것은 `L.unknown=[{c,i,t}]` 에 담아 둔다. 그 위젯을 아는
   버전이 되면 원래 칸·자리로 되돌린다. **앞으로 새 위젯 종류를 더할 때
   이 규칙을 깨지 말 것** — 깨면 같은 사고가 그대로 재현된다.
2. **나보다 낮은 버전이 쓴 문서는 믿지 않는다.** `appVer` 를 `_verCmp()` 로 비교해
   낮으면 rev 없는 문서와 **똑같이** 다룬다 — 백업 후 `_fbMerge(null, ST, remote, false)`
   로 되살리고 다시 올린다(`_fbHealFromLegacy`). 리스너와 `_fbCommit` **양쪽 모두**에 넣었다.
   ⚠️ 여기서도 base 를 넘기면 안 된다(7-2 의 경고와 같은 이유). 믿지 않은 문서를
   `_fbSetBase` 로 기준점 삼지도 않는다 — rev 만 올린다.
   `appVer` 가 아예 없으면(≤26-0802-0) 낡은 것으로 본다.
   **`_fbForceWrite` 도 `appVer` 를 남겨야 한다** — 안 남기면 남들이 "낡은 기기"로 오해한다.
3. **창을 앞으로 불러온 직후는 아직 "쓰는 기기"가 아니다.** 탭을 눌러 활성화한
   그 클릭이 곧바로 `_fbLastTouchTs` 를 갱신해, 하루 종일 열어만 뒀던 창이
   충돌에서 이겼다. 이제 `visibilitychange→visible` 후 20초(`_FB_WAKE_MS`) 동안은
   **실제 편집(`_fbLastEditTs`)이 있기 전까지** 유휴로 본다.
   ⚠️ `_fbVisibleSince` 의 초기값은 반드시 **0**. `Date.now()` 로 두면 앱을 켜고
   20초 안의 편집이 전부 밀린다 (`tests/test_merge.js`·`test_stale.js` 가 잡아냈다).
4. **낡은 탭 자가 갱신을 유휴 조건에서 풀었다.** 정작 사고를 내는 것은
   *사람이 방금 앞으로 불러온* 낡은 탭이라 유휴가 아니어서 갱신되지 않았다.
   이제 못 올린 편집이 없고 글자를 치는 중이 아니면 바로 새로고침한다.
5. **원격 반영 뒤 설정창 버튼 상태도 맞춘다** — 5-3(기준날짜/만 기간 버튼이
   둘 다 꺼져 보임)의 진짜 원인. `_fbApplyStateToApp` 이 화면만 다시 그리고
   설정창 안은 옛 표시 그대로 뒀다. 열려 있으면 `renderSettingsPanel()` /
   `renderVerseSettingsModal()` 을 다시 부른다.

**남는 한계 (HB에게 반드시 안내)** — 이미 열려 있는 **옛 탭에는 이 코드가 없다.**
그 탭은 여전히 모르는 것을 지운 채 올린다. 다만 이제 신버전 기기가 곧바로
되살려 다시 올리고, 그 탭도 새 버전을 보면 스스로 새로고침한다.
**버전을 올린 날에는 모든 기기를 한 번씩 새로고침하는 것이 여전히 가장 확실하다.**

### 7-3. 이 영역을 고칠 때의 체크리스트

- [ ] `_fbMerge`의 `preferCloud` 인자를 명시적으로 넘겼는가?
      (생략하면 `_fbDeviceIdle()` 자동 판정)
- [ ] `_mgPreferCloud`는 전역 플래그다. `_fbMerge`가 `finally`로 복원한다. 중첩 호출 주의.
- [ ] rev 없는 문서 경로에 base를 넘기지 않았는가? (위 경고)
- [ ] 리스너의 옛 가드(쓰기 대기 중 무시 / 2.5초 무시)를 되살리지 않았는가?
- [ ] **새 위젯·새 자료형을 더하면서 "모르는 값을 지우는" 코드를 넣지 않았는가?** (7-2-2)
- [ ] 낮은 버전이 쓴 문서 경로(`_fbVerIsOlder`)를 리스너·커밋 **양쪽** 다 지나가는가?
- [ ] 병합 시나리오 테스트를 돌렸는가? (`test_merge` `test_stale` `test_syncver` 전체 통과 확인)
- [ ] 백업 쓰기 실패가 동기화 자체를 막지 않는가? (catch로 삼켜야 함)

### 7-4. 관측할 것 (HB 보고 기준)

만약 덮어쓰기가 **또** 보고되면, 아래를 순서대로 확인한다.

0. **모든 기기의 버전이 같은가**를 가장 먼저 확인한다. 하나라도 낮으면 7-2-2다.
   콘솔에 `구버전 기기의 덮어쓰기를 감지해 복구했습니다 … 버전: v. 26-08xx-x` 가
   찍히고, 화면에도 그 버전이 적힌 토스트가 한 번 뜬다.
1. 사고 기기의 **버전 표시**를 먼저 물어본다 (설정에 표시됨).
   서로 다르면 7-2의 구버전 경로 문제 — 그 기기를 새로고침.
2. 브라우저 콘솔에 `구버전 기기의 덮어쓰기를 감지해 복구했습니다` 가 찍히는지.
   찍힌다면 방어는 작동 중이고, 원인 기기의 `dev` ID가 함께 나온다.
3. 계정 탭 → 데이터 복구 → **자동 백업 보기**로 직전 클라우드 원본 확인.
4. 그래도 재현되면 **양쪽 다 신버전인데 충돌**한 경우 —
   `_fbDeviceIdle()` 임계(2분)와 `_mgWhole`의 충돌 규칙을 다시 볼 것.

---
## 8. HB가 직접 해야 하는 외부 설정

| # | 할 일 | 상태 |
|---|-------|------|
| A | Firestore 규칙 `devNotes` | ✅ 완료 (**기존 규칙이 더 안전함 — 절대 건드리지 말 것**) |
| B | Firestore 규칙 `verseStats` | ✅ 완료 (2026-08-02 확인) |
| C | `versepush` 함수 + Cloud Scheduler | ✅ 완료 |
| D | **Node.js 20 → 22 런타임 업그레이드** | ⚠️ **미완료 · 2026-10-30 마감** |
| E | EmailJS Gmail 재연결 (412) | ✅ 완료 (2026-08-02 확인) |
| F | 도메인 `block7.my` 연결 | ✅ 완료 |
| G | `versepush` 의 `APP_URL` → `https://block7.my/` | ✅ 완료 (2026-08-02 확인) |
| H | Firebase 승인 도메인에 `block7.my` 추가 | ✅ 완료 (2026-08-02 확인) |

### 서버 코드 배포 방법 (2026-08-05 확립)

서버 쪽은 **두 군데가 서로 다른 방식**이라 헷갈리기 쉽다.

| 무엇 | 어디 | 어떻게 |
|---|---|---|
| `functions/index.js` (`block7Notify`, `block7TestPush`) | Firebase Functions v2 · **asia-northeast3** | 아래 Cloud Shell 절차 |
| `versepush/index.js` (말씀 푸시) | **Cloud Run 별도 서비스** | 콘솔에서 `소스 수정` → 붙여넣기 → `저장 후 다시 배포` |

**Firebase Functions — 구글 Cloud Shell (설치할 것 없음)**

```
git clone https://github.com/Underkut/block7.git
cd block7/functions
npm install            # ⚠️ 이걸 빼먹으면 "Couldn't find firebase-functions package" 로 죽는다
cd ..
firebase login --no-localhost
firebase deploy --only functions
```

- `firebase.json` · `.firebaserc` 가 저장소에 있어 프로젝트 지정이 필요 없다 (2026-08-05 추가)
- 자세히 볼 때는 `--debug` (`--info` 라는 옵션은 없다)
- `npm warn` · `EBADENGINE` · `deprecated` 는 전부 무시해도 된다
- 성공 표시: `✔ functions[block7Notify(asia-northeast3)] Successful update operation.`
- ⚠️ 배포 도중 **삭제(deletion)를 묻는 화면이 나오면 반드시 N**

### D — 유일하게 남은 것 (마감 있음)

할일 알림을 보내는 `block7Notify` 함수가 **Node.js 20** 위에서 도는데,
구글이 **2026-10-30에 이 런타임을 중단**한다. 그 전에 22로 올리지 않으면
**그날부터 할일 알림이 멈춘다.** 앱 코드와는 무관한 서버 쪽 설정이다.

작업 위치: 맥미니의 `functions` 폴더.
HB는 코더가 아니므로, 진행할 때 **`functions/package.json` 내용을 먼저 보여달라고
요청**한 뒤 정확한 수정 한 줄과 배포 명령을 짚어줄 것.
현재 런타임 확인법: Firebase 콘솔 → Functions → `block7Notify` 의 런타임 표시.

### Firestore 보안 규칙 관련 주의

- `users/{uid}/data/` 아래에 `state` 뿐 아니라 **`backup_0` ~ `backup_2`** 문서가
  생긴다(자동 백업). 규칙이 하위 문서 전체를 허용해야 백업이 저장된다.
- `devNotes` 규칙은 지금 것이 더 안전하다. **제안하지도 말고 건드리지도 말 것.**

---

## 9. 산출물 파일

- `index.html` / `index-dev.html` — 앱 본체
- `firebase-messaging-sw.js` — 푸시 서비스워커 (저장소 최상위)
- `manifest.json` — `start_url`/`scope` 는 **`/`** (루트 도메인이므로)
- `og-image.png` — 링크 공유 카드 이미지 (저장소 최상위)
- `404.html` — 점검 페이지

---

## 10. Claude Code에서 작업할 때

이 프로젝트는 **단일 거대 파일**(`index.html`, 약 100만 자)이라
Claude Code와 궁합이 좋다. 파일 전체를 매번 주고받을 필요가 없기 때문이다.

### 10-1. 지켜야 할 습관

- **파일 전체를 읽으려 하지 말 것.** `grep`으로 위치를 먼저 찾고
  필요한 구간만 읽는다. 통째로 읽으면 문맥이 순식간에 찬다.
- 편집은 **문자열 치환**으로. 줄 번호는 편집 한 번에 전부 밀린다.
- **한 번에 하나씩 고치고 그때마다 검증**한다. 몰아서 고치면 어디서 깨졌는지 못 찾는다.
- 커밋은 HB가 눈으로 확인한 **뒤에**. 검증만 통과했다고 바로 push 하지 말 것.

### 10-2. 저장소에 두면 좋은 것 (아직 없음 → 만들 것 권장)

```
tools/verify.py     ← 배포 전 4종 검증 (매 세션 새로 짜던 것)
tests/test_merge.js ← 3자 병합 20건
tests/test_dedupe.js← 중복 구절 정리 18건
tests/test_stale.js ← 구버전·유휴 기기 방어 14건
tests/test_sheet.js ← 시트 동기화·대량삭제 방어 23건
CLAUDE.md           ← 3장의 규칙을 옮겨두면 매번 읽어줌
```

`CLAUDE.md`는 Claude Code가 **매 세션 자동으로 읽는** 파일이다.
3장(매 작업 규칙)과 7장(동기화 주의)을 여기에 옮겨두면 인계 문서를
매번 지목하지 않아도 규칙이 지켜진다. 처음 한 번 만들어두면 계속 효과가 있다.

### 10-3. HB에게 결과를 보여주는 방법

HB는 코드를 읽지 않는다. 작업이 끝나면 항상:
1. **무엇이 문제였는지** 일상어로
2. **무엇을 바꿨는지** 기능 단위로
3. **기기에서 어떻게 확인하는지** 구체적 절차로

이 세 가지를 쓴다. 코드 조각을 붙여넣는 것은 도움이 되지 않는다.

---

## 11. 새 작업 시작 방법

**Claude Code (권장)**
1. 저장소 폴더에서 `claude` 실행
2. *"BLOCK7-작업인계.md 읽고 이어가자. 오늘 날짜 확인해서 버전 새로 시작해줘."*

**웹/앱 대화창**
1. 이 파일 + 최신 `index.html` 업로드
2. 같은 문장으로 시작

> 대화가 길어져 한계에 닿으면, 이 문서를 갱신해 달라고 한 뒤 새 대화로 옮긴다.
> 갱신할 때는 **2장 버전 이력**과 **5장 과제 상태**를 반드시 최신으로.
