# BLOCK7 — 작업 규칙

한국어 PWA(할일 관리 + 성경 말씀). **단일 파일** `index.html` 하나로 GitHub Pages 배포.
배포 주소 `https://block7.my/` · 저장소 `Underkut/block7` · Firebase `block7-8f24e`.

**개발자(HB)는 코더가 아니다.** 결과를 보고할 때는 코드가 아니라
① 무엇이 문제였는지 ② 무엇을 바꿨는지 ③ 기기에서 어떻게 확인하는지를 일상어로 쓴다.
외부 설정(Firebase 콘솔 등)은 클릭 순서를 하나하나 짚어준다. 대화는 한국어로.

자세한 배경·아키텍처·과거 사고 기록은 **`BLOCK7-작업인계.md`** 에 있다.
동기화나 구글 시트 쪽을 건드리기 전에는 그 문서의 해당 장을 먼저 읽는다.

## 매 작업마다

- **버전**: `v. YY-MMDD-N` (N = 그날 몇 번째 빌드). **반드시 실제 오늘 날짜로.**
  두 군데를 같은 값으로 — 2번째 줄 주석과 `const APP_VERSION`.
  이 값은 클라우드에도 기록되어 낡은 탭 자동 갱신 판정에 쓰이므로 형식을 바꾸지 말 것.
- **산출물 2개**: `index.html`(운영, `DEV_MODE=false`)와
  `index-dev.html`(`DEV_MODE=true`, 2번째 줄 `[DEV — no Firebase]`).
  개발본은 **손으로 만들지 말고 `./tools/make-dev.sh`** 로 만든다 (네 곳을 바꾼다).
  개발본도 저장소에 함께 커밋한다 → `https://block7.my/index-dev.html` 로 어느 기기에서나 열린다.
  아이폰은 그 주소에서 "홈 화면에 추가" 하면 **BLOCK7 DEV** 아이콘이 운영본과 따로 생긴다.
  ⚠️ 개발본은 Firebase 가 꺼져 있어 **푸시 알림은 개발본에서 확인할 수 없다** (운영본에서만).
- **배포 전 점검**: `./tools/check.sh` 를 돌리고 **전체 통과**를 확인한다. 실패하면 배포하지 않는다.
- **PR 병합은 클로드가 직접 한다** (2026-08-02부터). HB가 GitHub에서 버튼을 누를 필요 없다.
  안전장치는 "누가 병합하느냐"가 아니라 **"PR에 무엇을 담느냐"** 로 지킨다:
  - 새 작업은 **개발본(`index-dev.html`)만** 담은 PR로 올리고 바로 병합한다 →
    `block7.my/index-dev.html` 에서 HB가 확인한다. 운영 사이트는 그대로다.
  - **운영본(`index.html`)은 HB가 개발본으로 확인한 뒤에만** PR에 담는다.
    점검을 통과했다는 이유로 운영본을 먼저 올리지 않는다.
  - PR을 만들기 전에 항상 `git fetch origin main` 으로 **직전 PR이 이미 병합됐는지 확인**하고,
    병합됐으면 최신 `main` 에서 브랜치를 다시 딴다. (안 그러면 커밋이 어느 PR에도 안 들어간다)

## 이 저장소에서 일하는 법

- `index.html` 은 약 100만 자다. **통째로 읽지 말 것.** `grep`으로 위치를 찾고 필요한 구간만 읽는다.
- 편집은 문자열 치환으로. 줄 번호는 편집 한 번에 전부 밀린다.
- 한 번에 하나씩 고치고 그때마다 점검한다. 몰아서 고치면 어디서 깨졌는지 못 찾는다.
- HTML 블록을 지울 때는 `<div>` 균형을 반드시 다시 센다.
  (2026-07-31에 범위가 넘쳐 알림 탭 전체가 통째로 지워진 적이 있다)

## 절대 하지 말 것

- **데이터 계층(저장·동기화·시트)을 테스트 없이 고치지 말 것.** 손실이 즉시 발생한다.
  고칠 때는 `tests/` 에 시나리오를 먼저 추가한다.
- 리스너의 옛 가드(쓰기 대기 중 원격 무시 / 2.5초 내 편집이면 원격 무시)를 **되살리지 말 것.**
  그게 기기 간 데이터 손실의 원인이었다.
- rev 없는 클라우드 문서를 병합할 때 base 를 넘기지 말 것 (`_fbMerge(null, ...)`).
  base 를 주면 구버전 기기의 덮어쓰기가 그대로 통과한다.
- Firestore `devNotes` 보안 규칙은 지금 것이 더 안전하다. 제안하지도 건드리지도 말 것.
- 외부에서 받아온 데이터로 **지우는** 동작에는 반드시 규모 상한을 둘 것.
  "받아온 게 비어 있지 않다"는 안전 조건이 못 된다. (2026-08-02 대량 삭제 사고)

## 자주 걸리는 것들

- **iOS PWA**: 네이티브 `confirm()` 은 조용히 무시된다 → `appConfirm(msg, onYes)` 를 쓴다.
  복사는 탭 직후 **동기적으로** 시작해야 한다. `await` 뒤에 복사하면 실패한다.
- **안드로이드**: 뷰포트 폭은 `screen.width` 가 아니라 `documentElement.clientWidth`.
- **한국어 줄바꿈·폭 측정**: 폭은 컨테이너에서 잰다. 글자 요소에서 재면 계속 작아진다.
- `_refNorm()` 은 공백까지 제거한다. 정식 표기 판정은 `_refNorm(ref) === ref.replace(/\s+/g,'')`.
- `openVerseFull()` 은 인자 없이 부르면 지정 구절을 지운다. 특정 구절을 열 땐 `openVerseFull(true)`.

## UI 원칙

미니멀. 아이콘에 테두리·박스 금지. 요소를 나란히 늘리지 말고 **교체**한다.
보조 동작은 롱터치/우클릭으로 일관되게. 얇은 산세리프, 절제된 효과. 문구는 한국어 존댓말.
**큰 UI 변경은 먼저 제안하고 확인받는다.**

### 팝업·전체화면 페이지 (새로 만들 때도 반드시 이 형태로)

**닫기는 우상단 × 하나로 통일한다. 하단 '닫기' 버튼을 만들지 않는다.** (v26-0802-4에 전부 정리)

- × 는 `.modal-x` 클래스를 쓴다. 테두리·박스 없이 아이콘만 (18×18, `stroke-width:1.8`)
- 제목은 **가운데**. 좌측에 아이콘이 없으면 `.modal-head-gap` 으로 같은 폭을 비워 정중앙을 맞춘다
- 좌상단은 그 화면의 **보조 메뉴** 자리다 (예: '현재 말씀 모음'의 햄버거 = 모음 설정)
- 하단에는 **실행 버튼만** 남긴다 (예: 연락처의 '추가'). 취소·닫기는 × 가 대신한다

배치 두 가지 — 스크롤 방식에 따라 고른다:

| 상황 | 방법 |
|---|---|
| 팝업 안쪽만 스크롤 (`flex-direction:column` + 내부 `overflow:auto`) | `.modal-x` 그대로 (`position:absolute`). 제목에 `padding:0 28px` 로 × 와 안 겹치게 |
| 팝업 **전체**가 스크롤 (`overflow-y:auto`) | `.modal-head` (sticky 제목줄) 안에 `.modal-x.modal-x-inline` 을 넣는다 |

⚠️ 전체가 스크롤되는 팝업에 `.modal-x` 를 그냥 쓰면 **목록을 내릴 때 × 가 위로 밀려 사라져**
닫을 방법이 없어진다. `.contacts-modal` 계열이 여기 해당한다.

⚠️ 내용을 `innerHTML=''` 로 지우고 다시 그리는 팝업은 **× 도 같이 지워진다.** 그릴 때 함께 만들 것.

### 설정창 등급 (이지·미드·파워) — v26-0805-1

설정 항목이 너무 많아 처음 쓰는 사람이 질리지 않도록 세 단계로 보여줄 양을 조절한다.
등급은 `ST.settings.uiLevel` (`easy`/`mid`/`power`), 설정창 헤더의 아이콘 3개로 고른다.

**설정창에 새 항목을 만들 때는 `data-lv` 를 반드시 정한다.**

| 표기 | 뜻 |
|---|---|
| (없음) | 세 등급 모두 보인다 |
| `data-lv="mp"` | 미드·파워만 |
| `data-lv="p"` | 파워만 |

- 감출 때 `style.display` 를 건드리지 않는다. 개발자 전용처럼 따로 숨겨둔 항목까지
  되살아난다. `.lv-hide` 클래스만 붙였다 뗀다 (`_lvApplyIn`).
- 앞의 스위치를 켰을 때만 나오는 줄은 `data-notif-sub` / `data-cond` 로 따로 표시하고
  `_syncCondRows()` 가 맞춘다. 등급과 조건은 **서로 다른 클래스**라 둘 중 하나만
  걸려도 안 보인다.
- 탭을 통째로 감출 땐 `_stabList()` 를 고친다. 트랙 이동은 항상 **보이는 탭 목록**의
  자리로 계산한다 (`SETTINGS_TABS` 인덱스를 그대로 쓰면 이지에서 어긋난다).
- 기본값을 바꾸는 것은 `_settingsDefaults` 뿐이라 **이미 값이 있는 기기는 안 바뀐다.**

**새 팝업을 만들면 `_ESC_CLOSERS` 표에 `[id, 닫기함수]` 한 줄을 추가한다** (v26-0803-10).
PC 에서 ESC 는 항상 × 와 같은 일을 한다 — 겹쳐 있으면 **가장 위 것만** 닫히고,
계속 누르면 들어온 길을 그대로 되돌아 나온다. 순서는 화면의 z-index 를 실제로 재서
정하므로 표를 다시 정렬할 필요는 없다. opacity 로 여닫는 화면만 세 번째 자리에
열림 판정을 직접 준다 (`trashPanel` 처럼).

**전체화면은 이전 화면을 닫지 말고 "숨겼다 되살린다".** `openVerseFull()` 이
`_vfHideCoversNow()` 로 아래 화면들을 숨기고, `closeVerseFull()` 이 되살린다.
목록에서 말씀을 눌러 전체화면을 열 때 `closeVerseListModal()` 같은 걸 부르면
닫고 나왔을 때 목록이 사라진다. 새 화면에서 전체화면을 열 수 있게 만들면
`_vfHideCovers()` 안의 목록에 그 화면도 넣는다.
